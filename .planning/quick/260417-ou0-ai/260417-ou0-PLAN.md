---
phase: quick-260417-ou0
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/components/chat/chat-container.tsx
autonomous: false
requirements:
  - QUICK-260417-OU0-01
must_haves:
  truths:
    - "AI 응답 스트리밍 중 사용자가 위로 스크롤하면 스크롤이 강제로 하단으로 따라가지 않는다"
    - "사용자가 맨 아래에 있을 때 새 메시지를 전송하면 스트리밍이 하단을 따라간다 (기존 UX 유지)"
    - "사용자가 위로 스크롤한 뒤 다시 하단 근처(<80px)로 돌아오면 auto-scroll이 재개된다 (Slack/YouTube 패턴)"
    - "사용자가 새 메시지를 전송하면 auto-scroll이 다시 활성화되어 하단으로 이동한다"
  artifacts:
    - path: "frontend/src/components/chat/chat-container.tsx"
      provides: "조건부 auto-scroll with 사용자 의도 감지"
      contains: "stickToBottomRef"
  key_links:
    - from: "wheel/touchmove/keydown 이벤트"
      to: "stickToBottomRef"
      via: "사용자 의도 기반 auto-scroll 해제"
      pattern: "wheel.*stickToBottom"
    - from: "scroll 이벤트 (하단 복귀 감지)"
      to: "stickToBottomRef"
      via: "하단 근처 복귀 시에만 재활성화"
      pattern: "distanceFromBottom.*stickToBottom"
---

<objective>
AI 응답 스트리밍 중 자동 스크롤이 사용자의 스크롤 위치를 존중하도록 수정.

Purpose:
- 현재 구현(`chat-container.tsx:111-142`)에는 `stickToBottomRef` 기반 조건부 auto-scroll이 이미 있으나, 사용자 신고에 따르면 여전히 스트리밍 중 강제로 하단으로 끌려 내려온다.
- 근본 원인: `onScroll` 핸들러가 프로그램적 스크롤(`viewport.scrollTop = viewport.scrollHeight`)과 사용자 스크롤을 구분하지 않는다. 스트리밍 중 `scrollHeight`가 증가해도 `scrollTop`이 비슷하게 따라가면 `distanceFromBottom < 50`이 유지되어 `stickToBottomRef`가 `true`로 고정됨. 또한 프로그램적 스크롤 자체가 `scroll` 이벤트를 발생시켜 즉시 `stickToBottomRef=true`로 재설정하는 self-sustaining loop 존재.

Approach (플래너 판단 — constraints에서 #2 "조건부 auto-scroll" 선택):
- 글자 그대로의 "완전 비활성화"(#1)가 아닌 **Slack/YouTube 패턴(#2)** 을 채택. 이유:
  1. 빈 대화 → 첫 질문 송신 시 사용자는 하단에 있고, auto-scroll이 없으면 긴 응답이 화면 밖으로 나가버림 → UX 후퇴
  2. 사용자의 핵심 불만은 "위로 스크롤해 이전 내용을 읽을 때 강제로 끌려 내려오는 것" → 이 케이스만 막으면 됨
  3. 구현 복잡도가 #1과 동일(둘 다 `stickToBottomRef` 하나만 관리). #2가 상위 호환
- 핵심 변경: **사용자 의도 감지를 `scroll` 이벤트가 아닌 `wheel`/`touchmove`/`keydown`(PageUp/Up/Home)으로 이동**. 이렇게 하면 프로그램적 스크롤과 사용자 스크롤이 분리되어 self-sustaining loop가 끊어짐.
- 재활성화 조건: (a) 사용자가 하단 근처(<80px)로 다시 스크롤, 또는 (b) `sendMessage` 호출 시 (기존 라인 203 로직 유지)

Output:
- `chat-container.tsx` 내 `useEffect` 블록(119-142) 교체, 신규 `useRef` 없음 (기존 `stickToBottomRef` 재사용)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@frontend/CLAUDE.md
@frontend/AGENTS.md
@frontend/src/components/chat/chat-container.tsx
@frontend/src/components/ui/scroll-area.tsx

<interfaces>
<!-- chat-container.tsx 내 관련 기존 상태/ref (이미 존재) -->

```typescript
// Line 111-113
const scrollRef = useRef<HTMLDivElement>(null);       // ScrollArea Root
const stickToBottomRef = useRef(true);                // auto-scroll 활성 플래그 (재사용)
const prevLenRef = useRef(0);

// Line 115
const isLoading = status === "streaming" || status === "submitted";

// Line 203 (handleSubmit 내부) — 신규 메시지 송신 시 sticky 재활성화 (유지)
stickToBottomRef.current = true;
```

viewport 접근 패턴 (CLAUDE.md 명시 + scroll-area.tsx 확인):
- `scrollRef.current` → ScrollArea Root
- `scrollRef.current?.querySelector<HTMLElement>('[data-slot="scroll-area-viewport"]')` → 실제 scrollable element
</interfaces>

<current_implementation>
교체 대상 (chat-container.tsx:117-142):

```typescript
// 스크롤 위치 감지: 사용자가 위로 스크롤하면 자동 스크롤 중단,
// 하단 근처로 돌아오면 자동 스크롤 재개.
useEffect(() => {
  const viewport = scrollRef.current?.querySelector<HTMLElement>(
    '[data-slot="scroll-area-viewport"]',
  );
  if (!viewport) return;
  function onScroll() {
    const el = viewport!;
    const distanceFromBottom = el.scrollHeight - el.scrollTop - el.clientHeight;
    stickToBottomRef.current = distanceFromBottom < 50;
  }
  viewport.addEventListener("scroll", onScroll, { passive: true });
  return () => viewport.removeEventListener("scroll", onScroll);
}, []);

// 새 메시지/스트리밍 시 하단 스크롤 — 사용자가 위로 스크롤한 상태면 건너뜀.
useEffect(() => {
  if (!stickToBottomRef.current) return;
  const viewport = scrollRef.current?.querySelector<HTMLElement>(
    '[data-slot="scroll-area-viewport"]',
  );
  if (viewport) {
    viewport.scrollTop = viewport.scrollHeight;
  }
}, [messages]);
```

문제점:
1. `onScroll`이 프로그램적 스크롤에도 발동 → `stickToBottomRef`가 의도치 않게 재설정됨
2. 50px threshold가 스트리밍 중에는 너무 관대(new content가 계속 쌓이면서 경계를 넘나듦)
</current_implementation>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: 사용자 의도 기반 auto-scroll 해제 로직으로 교체</name>
  <files>frontend/src/components/chat/chat-container.tsx</files>
  <behavior>
    - 사용자가 wheel(마우스/트랙패드 스크롤), touchmove(모바일 스와이프), 또는 PageUp/ArrowUp/Home 키를 누르면:
      - 현재 스크롤 위치를 확인해서 하단에서 80px 이상 떨어져 있으면 `stickToBottomRef.current = false` 설정
      - 하단 근처면 아무것도 하지 않음 (사용자가 의도적으로 내린 경우라 sticky 유지가 맞음)
    - `scroll` 이벤트에서는 "하단 근처 복귀 감지"만 담당: `distanceFromBottom < 80` 이면 `stickToBottomRef.current = true` (프로그램적 스크롤도 하단에 도달시키는데, 이 경우 이미 true라서 no-op — 즉 누수 없음)
    - `useEffect([messages])` 블록은 기존 그대로 유지 — `stickToBottomRef.current`가 true일 때만 하단으로 스크롤
    - `sendMessage` 호출 후 `stickToBottomRef.current = true` (기존 라인 203 로직 유지)
  </behavior>
  <action>
현재 `chat-container.tsx`의 스크롤 관련 `useEffect` 블록(117-142라인)을 아래 내용으로 교체한다.

**교체 코드**:

```typescript
// 사용자 의도 감지: wheel/touch/keyboard로 위로 스크롤하면 auto-scroll 해제.
// scroll 이벤트가 아닌 입력 이벤트를 사용하는 이유:
// scroll 이벤트는 프로그램적 scrollTop 변경(아래 effect)에도 발동되어
// stickToBottom 플래그가 self-reset되는 루프가 생긴다. 입력 이벤트는
// 사용자의 실제 동작만 포착하므로 이 루프를 끊어준다.
useEffect(() => {
  const viewport = scrollRef.current?.querySelector<HTMLElement>(
    '[data-slot="scroll-area-viewport"]',
  );
  if (!viewport) return;

  function isAwayFromBottom(el: HTMLElement) {
    // 80px threshold: 스트리밍 중 새 청크로 scrollHeight가 급변해도
    // 사용자가 "충분히 위로" 올라간 케이스만 감지 (기존 50px은 너무 민감).
    return el.scrollHeight - el.scrollTop - el.clientHeight > 80;
  }

  // 사용자 제스처: 위로 스크롤 의도 감지 → sticky 해제
  function onUserScroll() {
    if (isAwayFromBottom(viewport!)) {
      stickToBottomRef.current = false;
    }
  }

  function onKey(e: KeyboardEvent) {
    // PageUp/ArrowUp/Home: 위로 이동 의도
    if (e.key === "PageUp" || e.key === "ArrowUp" || e.key === "Home") {
      // rAF로 한 프레임 지연 — 키가 적용된 후의 scrollTop을 읽기 위함
      requestAnimationFrame(() => {
        if (isAwayFromBottom(viewport!)) {
          stickToBottomRef.current = false;
        }
      });
    }
  }

  // 하단 복귀 감지: 사용자(또는 프로그램)가 하단 근처에 도달하면 sticky 재활성화.
  // 프로그램적 스크롤이 이를 트리거해도 이미 true → no-op이라 루프 없음.
  function onScroll() {
    const el = viewport!;
    const distanceFromBottom = el.scrollHeight - el.scrollTop - el.clientHeight;
    if (distanceFromBottom < 80) {
      stickToBottomRef.current = true;
    }
  }

  viewport.addEventListener("wheel", onUserScroll, { passive: true });
  viewport.addEventListener("touchmove", onUserScroll, { passive: true });
  viewport.addEventListener("keydown", onKey);
  viewport.addEventListener("scroll", onScroll, { passive: true });

  return () => {
    viewport.removeEventListener("wheel", onUserScroll);
    viewport.removeEventListener("touchmove", onUserScroll);
    viewport.removeEventListener("keydown", onKey);
    viewport.removeEventListener("scroll", onScroll);
  };
}, []);

// 새 메시지/스트리밍 시 하단 스크롤 — 사용자가 위로 스크롤한 상태면 건너뜀.
useEffect(() => {
  if (!stickToBottomRef.current) return;
  const viewport = scrollRef.current?.querySelector<HTMLElement>(
    '[data-slot="scroll-area-viewport"]',
  );
  if (viewport) {
    viewport.scrollTop = viewport.scrollHeight;
  }
}, [messages]);
```

**변경 요약**:
1. 기존 `onScroll` 한 개를 → `onUserScroll`(wheel/touchmove), `onKey`(keyboard), `onScroll`(하단 복귀만) 세 개로 분리
2. 사용자 의도 판정 threshold: 50px → 80px (스트리밍 변동성 완화)
3. `stickToBottomRef` 변경 로직:
   - 사용자 제스처 + 하단에서 80px+ 벗어남 → `false`
   - scroll 이벤트에서 하단 근처 복귀 → `true`
   - 그 외는 변경 없음 (프로그램적 스크롤은 `stickToBottomRef`를 false로 바꾸지 않음)
4. `useEffect([messages])` 로직은 **그대로** 유지 (이미 올바름)
5. 라인 203의 `stickToBottomRef.current = true;` (handleSubmit 내) 도 **그대로** 유지

**하지 말아야 할 것**:
- 새로운 `useRef` 추가 금지 — 기존 `stickToBottomRef`만 재사용
- `useEffect([messages])` 내부 로직 변경 금지
- `handleSubmit` 내 `stickToBottomRef.current = true` 제거/변경 금지
- `scrollIntoView()` 도입 금지 — 기존 `scrollTop = scrollHeight` 패턴 유지 (ScrollArea Viewport 구조 때문)
- `behavior: "smooth"` 도입 금지 — 스트리밍 중 smooth scroll은 다음 청크와 충돌해 rubber-band 발생
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit</automated>
    TypeScript 컴파일 에러 없음. ESLint(혹은 Next.js의 내장 린트)도 `exhaustive-deps` 등 경고 없이 통과.
  </verify>
  <done>
    - `chat-container.tsx`의 스크롤 관련 `useEffect` 블록이 위 코드로 교체됨
    - `npx tsc --noEmit` 통과
    - 기존 `stickToBottomRef`, `scrollRef`, `handleSubmit` 내 `stickToBottomRef.current = true` 유지
    - 신규 `useRef` 추가 없음
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: 로컬 dev 서버에서 UX 검증</name>
  <what-built>
    스트리밍 중 사용자 스크롤 존중 로직. wheel/touchmove/keyboard 기반 의도 감지로 전환.
  </what-built>
  <how-to-verify>
    **⚠️ dev 포트는 3001 (CLAUDE.md 규정: 3000은 다른 앱이 점유 가능)**

    ```bash
    cd frontend && npm run dev -- -p 3001
    ```

    브라우저에서 `http://localhost:3001` 접속 후 로그인 → 기존 대화 또는 신규 대화 열기.

    **시나리오 A: 긴 응답 스트리밍 중 위로 스크롤 (핵심 수정 대상)**
    1. 긴 답변이 나올 질문을 전송 (예: "근로기준법 제60조 연차휴가 조문과 관련 판례 3개 요약해줘")
    2. 스트리밍이 시작되어 답변이 쌓이는 동안, 마우스 휠이나 트랙패드로 **위로 스크롤**
    3. ✅ 기대: 스크롤이 위에 머물러 있음. 스트리밍 청크가 도착해도 강제로 하단으로 내려오지 않음
    4. ❌ 실패 조건: 스트리밍 청크마다 하단으로 끌려 내려감 → 수정 실패

    **시나리오 B: 하단 복귀 시 auto-scroll 재개**
    1. 시나리오 A에서 위로 스크롤한 상태에서, 다시 스크롤을 끝까지 아래로 내림
    2. ✅ 기대: 이후 도착하는 스트리밍 청크들이 자연스럽게 하단을 따라감 (sticky 재활성화)

    **시나리오 C: 신규 메시지 송신 시 하단으로 이동**
    1. 시나리오 A에서 위로 스크롤한 상태에서, 입력창에 새 질문을 타이핑하고 전송
    2. ✅ 기대: 송신 즉시 스크롤이 하단으로 이동하고, 스트리밍도 하단을 따라감 (`handleSubmit`의 `stickToBottomRef.current = true` 동작)

    **시나리오 D: 키보드 스크롤**
    1. 긴 답변이 있는 대화에서, 스크롤 영역에 포커스를 주고 **PageUp** 또는 **↑** 키로 위로 이동
    2. 스트리밍이 진행 중이면: ✅ 기대 — auto-scroll 해제됨

    **시나리오 E: 모바일/터치 (선택, 가능하면)**
    1. 개발자 도구에서 모바일 에뮬레이션 활성화 또는 실제 터치 디바이스
    2. 스트리밍 중 스와이프로 위로 이동
    3. ✅ 기대: 스크롤이 위에 머무름

    **시나리오 F: 기존 UX 회귀 없음**
    1. 빈 대화에서 새 질문 전송 → 스트리밍이 하단을 따라가야 함 (기존 동작 유지)
    2. 대화 중간에서 맨 아래에 있을 때 새 청크 도착 → 따라가야 함
  </how-to-verify>
  <resume-signal>모든 시나리오 통과하면 "approved". 특정 시나리오 실패 시 어떤 시나리오의 어떤 동작이 어긋났는지 설명. (push 금지 — 사용자 확인 후에만 커밋/푸시 진행)</resume-signal>
</task>

</tasks>

<verification>
- 타입 체크: `cd frontend && npx tsc --noEmit` 통과
- 수동 UX 검증: 위 시나리오 A-F 모두 통과
- 코드 라인 수 델타: 약 +20 ~ +25라인 (단일 파일)
- 신규 import 없음, 신규 ref 없음
</verification>

<success_criteria>
- ✅ 스트리밍 중 사용자가 위로 스크롤한 상태에서, 스트리밍 청크가 도착해도 강제로 하단으로 끌려 내려오지 않는다
- ✅ 사용자가 하단으로 복귀하면 auto-scroll이 재개된다 (Slack 패턴)
- ✅ 신규 메시지 송신 시 auto-scroll이 재활성화되어 하단으로 이동한다
- ✅ 키보드 PageUp/↑/Home도 사용자 의도로 인식된다
- ✅ `tsc --noEmit` 통과, 신규 warning 없음
</success_criteria>

<output>
After completion, create `.planning/quick/260417-ou0-ai/260417-ou0-SUMMARY.md`.

SUMMARY에는 다음 포함:
- 변경한 파일 및 라인 범위
- 최종 `useEffect` 코드
- 사용자가 검증한 시나리오 결과 (A-F)
- 남은 후속 아이디어 (예: "새 메시지 ↓" 플로팅 버튼 — 이번 스코프 아님)
</output>
