---
phase: 02-streaming-stability-error-ux
plan: 01
name: route-ts-server-hardening
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/app/api/chat/route.ts
requirements: [STRE-01, STRE-02, STRE-04, STRE-06, STRE-07]
autonomous: true
commit_subject: "feat(02-01): route.ts MCP 타임아웃 + 캐시 + 구조화 에러 + SYSTEM_PROMPT 완화"
user_setup: []

must_haves:
  truths:
    - "createMCPClient 호출이 5초 안에 resolve되지 않으면 Promise.race가 Error('mcp_timeout')으로 reject되고 서버는 degraded mode로 진행한다"
    - "MCP tools() 결과가 동일 모듈 스코프 인스턴스 내에서 5분(300_000ms) TTL로 재사용되며, cache hit/miss가 임시 로그(`[mcp-cache] ...`)로 관찰된다"
    - "mcpClient 인스턴스도 tools와 함께 모듈 스코프에 캐싱되어 tool.execute closure가 stale client에 bind되지 않는다"
    - "createMCPClient 또는 tools() 호출에서 503/429/'Max sessions'가 감지되면 1초 대기 후 1회 재시도한다"
    - "Pre-stream 에러(createMCPClient 실패, tools() 실패)는 JSON body `{error:{code, message}}` + HTTP status(503/504/500)로 반환된다"
    - "에러 code는 5개 중 하나: 'mcp_timeout' | 'mcp_busy' | 'mcp_offline' | 'stream_timeout' | 'unknown'"
    - "toUIMessageStreamResponse onError 콜백은 JSON.stringify({error:{code, message}}) 형태 문자열을 반환한다 (LAW_API_KEY 리댁션 유지)"
    - "SYSTEM_PROMPT는 기존 '절대 규칙' 블록을 삭제하지 않고 일상 인사 예외 문장이 병기되어 있다"
    - "export const maxDuration = 60 은 그대로 유지되며, Phase 2 의도를 드러내는 한국어 주석이 동반된다"
  artifacts:
    - path: "frontend/src/app/api/chat/route.ts"
      provides: "MCP timeout + tools cache + classify + retry + structured error + SYSTEM_PROMPT relaxation"
      contains:
        - "let cachedClient"
        - "let cachedTools"
        - "let cachedAt"
        - "const CACHE_TTL_MS"
        - "mcp_timeout"
        - "classifyMcpError"
        - "classifyStreamError"
        - "makeErrorResponse"
        - "일상 인사"
        - "[mcp-cache]"
  key_links:
    - from: "frontend/src/app/api/chat/route.ts (POST handler)"
      to: "cachedClient / cachedTools 모듈 스코프"
      via: "getOrCreateMcp()"
      pattern: "getOrCreateMcp\\(\\)"
    - from: "frontend/src/app/api/chat/route.ts (POST handler)"
      to: "makeErrorResponse()"
      via: "pre-stream catch 경로"
      pattern: "makeErrorResponse\\("
    - from: "frontend/src/app/api/chat/route.ts (streamText.onError)"
      to: "JSON.stringify({error:{...}})"
      via: "toUIMessageStreamResponse.onError 반환"
      pattern: "JSON\\.stringify\\(\\{\\s*error:"
---

<objective>
route.ts 서버 측을 Phase 2 전체 실패 모드 계약에 맞춰 hardening한다. 단일 파일, 단일 커밋.

Purpose:
- Phase 1의 route.ts 구조(`stopWhen: stepCountIs(8)` + `closeMcp` + `consumeSseStream/onError`) 위에
  MCP 연결 5초 타임아웃(D-01), tools 캐싱(D-02), 자동 1회 503 재시도(D-08), 에러 분류/구조화 JSON 반환(D-05/D-11),
  SYSTEM_PROMPT 일상 인사 예외(D-14), warm-container PoC 임시 로그(D-04)를 한 번에 적용한다.
- Plan 02-02 클라이언트 에러 UX가 파싱할 표준 JSON 계약을 여기서 확정한다.

Output:
- `frontend/src/app/api/chat/route.ts` (단일 파일) 이 모든 구조화 변경을 포함한다.
- 새 파일 생성 없음. 새 import는 `import type { MCPClient } from "@ai-sdk/mcp"` 정도로 최소화.
</objective>

<execution_context>
@C:/Users/GLLUGA/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/GLLUGA/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/02-streaming-stability-error-ux/02-CONTEXT.md
@.planning/phases/02-streaming-stability-error-ux/02-RESEARCH.md
@.planning/phases/02-streaming-stability-error-ux/02-VALIDATION.md
@.planning/phases/01-empty-message-bug-fix-parts-contract/01-01-SUMMARY.md
@frontend/src/app/api/chat/route.ts
@frontend/CLAUDE.md
@frontend/AGENTS.md

<interfaces>
<!--
  현재 route.ts(Phase 1 완료본, 133 lines) 핵심 계약.
  이 플랜은 아래 계약을 모두 보존하면서 기능을 확장한다.
-->

From frontend/src/app/api/chat/route.ts (post-Phase-1 state):
```typescript
import { streamText, convertToModelMessages, stepCountIs } from "ai";
import { google } from "@ai-sdk/google";
import { createMCPClient } from "@ai-sdk/mcp";

export const maxDuration = 60;

const SYSTEM_PROMPT = `당신은 한국 법령 전문 어시스턴트...
━━━ 절대 규칙 (위반 금지) ━━━
- 법령 조문, 판례, 법률 해석을 답변할 때 반드시 도구를 호출하여 확인한 내용만 답변하세요.
- 도구 호출 없이 법령 내용을 답변하는 것은 금지합니다. 기억에 의존하지 마세요.
- 도구로 확인하지 못한 내용은 반드시 "⚠️ 확인되지 않은 정보"라고 명시하세요.
- 모든 법령/판례 인용에 출처를 표시하세요: [출처: 도구 검색 결과] 또는 [⚠️ 미확인]
- 도구 검색 결과에 없는 내용을 추가하거나 꾸며내지 마세요.
━━━━━━━━━━━━━━━━━━━━━━
규칙:
1. 법령 관련 질문을 받으면 먼저 도구를 호출하세요. 절대 기억으로 답변하지 마세요.
...`;

function getMcpUrl(): string {
  const key = process.env.LAW_API_KEY;
  if (!key) throw new Error("LAW_API_KEY 환경변수가 설정되지 않았습니다.");
  return `https://glluga-law-mcp.fly.dev/mcp?oc=${key}`;
}

export async function POST(req: Request) {
  const { messages: uiMessages, modelId } = await req.json();
  const selectedModel = modelId || "gemini-2.5-flash";
  const messages = await convertToModelMessages(uiMessages);

  let mcpClient: Awaited<ReturnType<typeof createMCPClient>> | undefined;
  let tools = {};
  try {
    mcpClient = await createMCPClient({
      transport: { type: "http", url: getMcpUrl() },
    });
    tools = await mcpClient.tools();
  } catch (e) {
    const errMsg = e instanceof Error ? e.message : String(e);
    if (errMsg.includes("503") || errMsg.includes("Max sessions") || errMsg.includes("429")) {
      return new Response(
        JSON.stringify({ error: "법령 검색 서버가 현재 혼잡합니다. 잠시 후 다시 시도해주세요." }),
        { status: 503, headers: { "Content-Type": "application/json" } }
      );
    }
    console.error("MCP 연결 실패, 도구 없이 진행:", errMsg);
  }

  const closeMcp = async () => {
    if (!mcpClient) return;
    try { await mcpClient.close(); }
    catch (closeErr) { console.error("mcpClient.close() failed:", closeErr); }
  };

  const result = streamText({
    model: google(selectedModel),
    system: SYSTEM_PROMPT,
    messages,
    stopWhen: stepCountIs(8),
    ...(Object.keys(tools).length > 0 ? { tools } : {}),
    onFinish: async ({ finishReason }) => {
      console.log("[route.ts] streamText finishReason:", finishReason);
      await closeMcp();
    },
    onError: async ({ error }) => {
      console.error("[route.ts] streamText error:", error);
      await closeMcp();
    },
  });

  return result.toUIMessageStreamResponse({
    consumeSseStream: async ({ stream }) => {
      const reader = stream.getReader();
      try {
        while (true) {
          const { done } = await reader.read();
          if (done) break;
        }
      } finally {
        reader.releaseLock();
      }
    },
    onError: (error) => {
      console.error("[route.ts] toUIMessageStreamResponse error:", error);
      let msg: string;
      if (error instanceof Error) msg = error.message;
      else if (typeof error === "string") msg = error;
      else msg = "알 수 없는 오류가 발생했습니다.";
      return msg.replace(/oc=[^&\s"]+/g, "oc=REDACTED");
    },
  });
}
```

From frontend/node_modules/@ai-sdk/mcp/dist/index.d.ts (VERIFIED in RESEARCH §1.1):
```typescript
interface MCPClientConfig {
  transport: MCPTransportConfig | MCPTransport;
  onUncaughtError?: (error: unknown) => void;
  name?: string;
  version?: string;
  capabilities?: ClientCapabilities;
}
// createMCPClient는 signal/timeout 필드 없음 → Promise.race로만 타임아웃 enforce 가능.
declare function createMCPClient(config: MCPClientConfig): Promise<MCPClient>;

interface MCPClient {
  tools<TOOL_SCHEMAS extends ToolSchemas = 'automatic'>(options?: {
    schemas?: TOOL_SCHEMAS;
  }): Promise<McpToolSet<TOOL_SCHEMAS>>;
  close(): Promise<void>;
  // ...
}
```

From frontend/node_modules/ai/dist/index.d.ts (VERIFIED in RESEARCH §3.2):
```typescript
// toUIMessageStreamResponse({ onError }) 는 (error) => string 시그너처.
// 반환된 string이 SSE error chunk의 errorText로 흘러가, 클라이언트 useChat.error.message로 도달.
```
</interfaces>
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → /api/chat | 사용자가 보낸 message 본문 + modelId (인증은 NextAuth가 상위에서 처리, 본 파일 스코프 아님) |
| /api/chat → 원격 MCP (`glluga-law-mcp.fly.dev`) | LAW_API_KEY 를 query param으로 전달. 응답 본문에 쿼리가 echo될 여지 |
| /api/chat → Gemini provider | 사용자 message 텍스트가 외부 provider로 전달 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01-01 | I (Information Disclosure) | toUIMessageStreamResponse onError 반환 문자열 | mitigate | JSON.stringify 시 `message` 필드는 미리 정의된 한국어 상수만 포함. 원시 error.message는 서버 console.error로만 흐른다. 기존 `/oc=[^&\s"]+/g → oc=REDACTED` 치환은 유지. |
| T-02-01-02 | I | pre-stream makeErrorResponse JSON body | mitigate | `code`는 enum 고정값, `message`는 미리 정의된 한국어 상수만. err.message/err.stack 절대 포함 금지. |
| T-02-01-03 | I | D-04 PoC 임시 `console.log('[mcp-cache]', ...)` | accept | cache miss/hit 표시만 로그. URL/키는 포함하지 않음. 02-03 단계에서 제거됨. |
| T-02-01-04 | D (Denial of Service) | cache stampede — 동시 cache miss 요청이 각자 createMCPClient 호출 → MCP 503 유발 | mitigate | `cachedClientPromise` pending-promise 패턴으로 동시 miss가 하나의 promise를 await하도록 한다 (RESEARCH §2.2 권장). |
| T-02-01-05 | D | Promise.race timeout 뒤 race loser가 live TCP 연결을 점유 | accept | @ai-sdk/mcp가 signal을 지원하지 않으므로 best-effort cleanup(`.then(c => c?.close().catch(() => {}))`)만 수행. MCP 서버의 idle timeout에 의존. RESEARCH §1.3 permit. |
| T-02-01-06 | T (Tampering) | SYSTEM_PROMPT 일상 인사 예외가 법령 질문까지 넓혀져 hallucination 발생 | mitigate | 기존 "절대 규칙" 섹션 완전 보존. 예외 문장은 "일상 인사", "메타 질문(이름, 기능)"에 한정하고 "법률 내용 질문에만 도구를 호출" 문장을 함께 명시. 02-03 UAT에서 법령 질문 5개 + 인사 5개로 cross-check. |
| T-02-01-07 | E (Elevation) | streamText onError 내부 closeMcp 누수 | mitigate | closeMcp는 try/catch로 감싸져 있어 unhandled rejection 발생 불가 (Phase 1 확립 패턴). 본 플랜에서 그대로 유지. |
</threat_model>

<tasks>

<task type="auto" tdd="false">
  <name>Task 02-01-01: SYSTEM_PROMPT 일상 인사 예외 문장 추가 (D-14)</name>
  <files>frontend/src/app/api/chat/route.ts</files>
  <read_first>
    - frontend/src/app/api/chat/route.ts (line 8-40, SYSTEM_PROMPT 상수 전체)
    - .planning/phases/02-streaming-stability-error-ux/02-CONTEXT.md (D-14 원문)
    - .planning/phases/02-streaming-stability-error-ux/02-RESEARCH.md §7 (Gemini tool bias 분석)
  </read_first>
  <behavior>
    - 기존 "━━━ 절대 규칙 (위반 금지) ━━━" 블록의 본문(5개 bullet)은 **전혀 삭제하지 않는다**.
    - 절대 규칙 블록 마지막 bullet 다음 줄, 블록 종료선(━━━) 바로 위에 한 줄 추가:
      `- 단, '안녕하세요', '고마워요' 같은 일상 인사나 봇 자체에 대한 메타 질문(이름, 기능)에는 도구를 호출하지 않고 자연스럽게 답변하세요. 도구는 법령·시행령·시행규칙·판례·행정규칙 등 **법률 내용 질문**에만 호출합니다.`
    - 다른 섹션(규칙, 쉬운 설명 원칙, 계약서/규정 검토 요청 시, 답변 형식)은 건드리지 않는다.
  </behavior>
  <action>
    `frontend/src/app/api/chat/route.ts`의 `const SYSTEM_PROMPT = \`...\`` 템플릿 리터럴을 열고,
    "━━━ 절대 규칙 (위반 금지) ━━━" 섹션 안의 마지막 bullet(`- 도구 검색 결과에 없는 내용을 추가하거나 꾸며내지 마세요.`) 아래에 정확히 아래 한 줄을 추가한다. 종료선 `━━━━━━━━━━━━━━━━━━━━━━`는 이 새 줄 **뒤**에 위치해야 한다.

    추가 문자열 (복사 가능):
    ```
    - 단, '안녕하세요', '고마워요' 같은 일상 인사나 봇 자체에 대한 메타 질문(이름, 기능)에는 도구를 호출하지 않고 자연스럽게 답변하세요. 도구는 법령·시행령·시행규칙·판례·행정규칙 등 **법률 내용 질문**에만 호출합니다.
    ```

    Edit tool로 기존:
    `- 도구 검색 결과에 없는 내용을 추가하거나 꾸며내지 마세요.\n━━━━━━━━━━━━━━━━━━━━━━`
    을
    `- 도구 검색 결과에 없는 내용을 추가하거나 꾸며내지 마세요.\n- 단, '안녕하세요', '고마워요' 같은 일상 인사나 봇 자체에 대한 메타 질문(이름, 기능)에는 도구를 호출하지 않고 자연스럽게 답변하세요. 도구는 법령·시행령·시행규칙·판례·행정규칙 등 **법률 내용 질문**에만 호출합니다.\n━━━━━━━━━━━━━━━━━━━━━━`
    로 치환.
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "안녕하세요" frontend/src/app/api/chat/route.ts` → 1개 match (SYSTEM_PROMPT 내부)
    - `grep -n "법률 내용 질문" frontend/src/app/api/chat/route.ts` → 1개 match
    - `grep -c "━━━ 절대 규칙 (위반 금지) ━━━" frontend/src/app/api/chat/route.ts` → 1 (블록 시작선 보존)
    - `grep -c "도구 검색 결과에 없는 내용을 추가하거나 꾸며내지 마세요" frontend/src/app/api/chat/route.ts` → 1 (기존 마지막 bullet 보존)
    - `cd frontend && npx tsc --noEmit` exit 0
    - SYSTEM_PROMPT의 "규칙 1", "쉬운 설명 원칙", "계약서/규정 검토", "답변 형식" 섹션 문자열은 변경 없음 (git diff에서 해당 라인 hunk 없음)
  </acceptance_criteria>
  <done>SYSTEM_PROMPT에 일상 인사 예외 1줄이 병기되고 기존 절대 규칙은 완전 보존된다. tsc 통과.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 02-01-02: MCP 타임아웃 + 캐시 + 재시도 + 에러 분류 + 구조화 Response 전체 구현 (D-01, D-02, D-04, D-05, D-08, D-11, D-12)</name>
  <files>frontend/src/app/api/chat/route.ts</files>
  <read_first>
    - frontend/src/app/api/chat/route.ts (Task 01에서 수정된 상태 전체)
    - frontend/node_modules/@ai-sdk/mcp/dist/index.d.ts (createMCPClient + MCPClient 타입 확인; RESEARCH §1에 인용됨)
    - frontend/node_modules/ai/dist/index.d.ts (streamText / toUIMessageStreamResponse onError 시그너처; RESEARCH §3.2에 인용됨)
    - .planning/phases/02-streaming-stability-error-ux/02-CONTEXT.md (D-01, D-02, D-05, D-08, D-11, D-12)
    - .planning/phases/02-streaming-stability-error-ux/02-RESEARCH.md §1, §2, §3, §6 (검증된 코드 샘플)
    - .planning/phases/02-streaming-stability-error-ux/02-CONTEXT.md canonical_refs (D-04 PoC 절차)
  </read_first>
  <action>
    `frontend/src/app/api/chat/route.ts`의 POST 핸들러를 아래 구조로 재작성한다. 한 번에 모든 구조화 변경을 적용. 파일 외 변경 없음.

    ### (A) 최상단 import 보강 (import type만 추가)
    기존 imports 그대로 유지:
    ```typescript
    import { streamText, convertToModelMessages, stepCountIs } from "ai";
    import { google } from "@ai-sdk/google";
    import { createMCPClient } from "@ai-sdk/mcp";
    import type { MCPClient } from "@ai-sdk/mcp";
    ```

    ### (B) maxDuration 선언에 D-12 근거 주석 추가
    ```typescript
    // D-12: Vercel serverless 60초 유지. Phase 2 스코프에서는 상향하지 않음.
    // Fluid Compute 활성화 여부와 stream_timeout UX 방어는 02-03 UAT에서 재검증.
    export const maxDuration = 60;
    ```

    ### (C) 모듈 스코프 캐시 + 에러 타입 블록 (SYSTEM_PROMPT 위 또는 아래 파일 최상부 상수 영역)
    SYSTEM_PROMPT 상수 **뒤**, `getMcpUrl()` 함수 **위**에 다음 블록 삽입:

    ```typescript
    // ─────────────────────────────────────────────────────────
    // Phase 2: MCP 모듈 스코프 캐시 (D-02) + pending-promise stampede 방어
    // ─────────────────────────────────────────────────────────
    // RESEARCH §2.2: cache stampede 방지를 위해 클라이언트 자체를 pending Promise
    // 상태로도 저장한다. 동시 요청이 하나의 connect를 await하도록 한다.
    // tool.execute closure가 stale client에 bind되지 않도록 mcpClient 인스턴스도
    // tools와 함께 같은 TTL에 묶어 저장한다 (RESEARCH §2.2 옵션 1).
    const CACHE_TTL_MS = 5 * 60 * 1000;
    let cachedClientPromise: Promise<{ client: MCPClient; tools: Record<string, unknown> }> | null = null;
    let cachedAt = 0;

    type ErrorCode =
      | "mcp_timeout"
      | "mcp_busy"
      | "mcp_offline"
      | "stream_timeout"
      | "unknown";

    // ─── 메시지 source-of-truth 규약 ─────────────────────────────
    // Client(frontend/src/lib/error-messages.ts의 KOREAN_MESSAGES)가 사용자에게
    // 렌더되는 canonical 문자열의 유일한 소스다. 서버의 KOREAN_ERROR_MESSAGES는
    // **서버 내부 debug log 용도**로만 사용되며, client parseChatError는 body의
    // `code` 필드만 읽고 `message` 필드는 무시한다 (client 측 KOREAN_MESSAGES로
    // 치환). 두 테이블의 문자열은 drift 허용 — 규약상 client가 항상 이김.
    // 이 주석을 제거하거나 서버/클라이언트 문자열을 동기화하려 하지 말 것.
    // 규약 변경 시 frontend/src/lib/error-messages.ts 상단 주석도 함께 갱신할 것.
    const KOREAN_ERROR_MESSAGES: Record<ErrorCode, string> = {
      mcp_timeout: "법령 검색 서버 연결이 지연되어 일반 답변만 드립니다.",
      mcp_busy: "법령 검색 서버가 현재 혼잡합니다. 잠시 후 다시 시도해주세요.",
      mcp_offline: "법령 검색 서버에 연결할 수 없어 일반 답변만 드릴 수 있습니다.",
      stream_timeout: "응답 생성 시간이 초과되었습니다. 질문을 더 간단히 해보세요.",
      unknown: "알 수 없는 오류가 발생했습니다. 새로고침 후 다시 시도해주세요.",
    };
    ```

    ### (D) 에러 분류 함수 (getMcpUrl 아래, POST 함수 위)
    ```typescript
    // D-11: createMCPClient / tools() throw의 에러 원인을 5개 code 중 하나로 분류.
    function classifyMcpError(err: unknown): ErrorCode {
      if (!(err instanceof Error)) return "unknown";
      const msg = err.message ?? "";
      if (msg.includes("mcp_timeout")) return "mcp_timeout";
      if (msg.includes("503") || msg.includes("429") || /Max sessions/i.test(msg)) return "mcp_busy";
      if (msg.includes("ENOTFOUND") || msg.includes("ECONNREFUSED") || msg.includes("fetch failed")) {
        return "mcp_offline";
      }
      if (err.name === "MCPClientError") return "mcp_offline";
      const cause = (err as { cause?: unknown }).cause;
      if (cause && typeof cause === "object" && cause !== null && "code" in cause) {
        const code = (cause as { code: unknown }).code;
        if (code === "ECONNREFUSED" || code === "ENOTFOUND") return "mcp_offline";
      }
      return "unknown";
    }

    // stream 도중 발생한 에러를 'stream_timeout' | 'unknown' 중 하나로 분류.
    function classifyStreamError(err: unknown): Exclude<ErrorCode, "mcp_timeout" | "mcp_busy" | "mcp_offline"> {
      if (!(err instanceof Error)) return "unknown";
      if (err.name === "AbortError" || /aborted/i.test(err.message ?? "")) return "stream_timeout";
      if (/timeout/i.test(err.message ?? "")) return "stream_timeout";
      return "unknown";
    }

    // pre-stream 에러에 대해 구조화 Response 반환. HTTP status는 관측용 (클라이언트는 body만 사용).
    function makeErrorResponse(code: ErrorCode, status: number): Response {
      return new Response(
        JSON.stringify({ error: { code, message: KOREAN_ERROR_MESSAGES[code] } }),
        { status, headers: { "Content-Type": "application/json" } }
      );
    }
    ```

    ### (E) MCP 연결 helper — Promise.race 5초 타임아웃 + 503 1회 재시도 + pending-promise 캐시 (D-01, D-02, D-08)
    ```typescript
    // D-01: createMCPClient에는 signal/timeout 파라미터가 없다 (RESEARCH §1.1 VERIFIED).
    // Promise.race만이 init 단계에서 타임아웃을 enforce할 수 있는 유일한 수단.
    async function raceWithTimeout(): Promise<MCPClient> {
      let lateClient: MCPClient | null = null;
      try {
        const client = await Promise.race<MCPClient>([
          createMCPClient({
            transport: { type: "http", url: getMcpUrl() },
          }).then((c) => {
            lateClient = c;
            return c;
          }),
          new Promise<never>((_, reject) =>
            setTimeout(() => reject(new Error("mcp_timeout")), 5000)
          ),
        ]);
        return client;
      } catch (err) {
        // Best-effort cleanup: race loser가 이후 resolve되면 조용히 close (RESEARCH §1.3).
        queueMicrotask(() => {
          void lateClient?.close().catch(() => {});
        });
        throw err;
      }
    }

    // D-08: createMCPClient + tools() 조합에서 'mcp_busy'가 감지되면 1초 대기 후 1회 재시도.
    async function connectMcpOnce(): Promise<{ client: MCPClient; tools: Record<string, unknown> }> {
      const client = await raceWithTimeout();
      try {
        const tools = (await client.tools()) as Record<string, unknown>;
        return { client, tools };
      } catch (toolsErr) {
        // tools() 실패는 우리가 client를 close할 책임을 진다.
        await client.close().catch(() => {});
        throw toolsErr;
      }
    }

    async function connectMcpWithRetry(): Promise<{ client: MCPClient; tools: Record<string, unknown> }> {
      try {
        return await connectMcpOnce();
      } catch (e) {
        if (classifyMcpError(e) !== "mcp_busy") throw e;
        // D-08: 1초 대기 후 1회 retry. 임시 관측 로그.
        console.log("[route.ts] mcp retry after 1s (503/Max sessions detected)");
        await new Promise((r) => setTimeout(r, 1000));
        return await connectMcpOnce();
      }
    }

    // D-02 + RESEARCH §2.2 pending-promise 캐시 패턴.
    // D-04: cache hit/miss는 임시 log로 관측. 02-03 Task 01에서 제거.
    async function getOrCreateMcp(): Promise<{ client: MCPClient; tools: Record<string, unknown> }> {
      const now = Date.now();
      if (cachedClientPromise && now - cachedAt < CACHE_TTL_MS) {
        console.log("[mcp-cache]", { hit: true, age: now - cachedAt });
        return cachedClientPromise;
      }
      console.log("[mcp-cache]", { hit: false, age: now - cachedAt });
      cachedAt = now;
      cachedClientPromise = connectMcpWithRetry();
      try {
        return await cachedClientPromise;
      } catch (e) {
        // 실패 시 다음 요청이 재시도 가능하도록 캐시 clear.
        cachedClientPromise = null;
        cachedAt = 0;
        throw e;
      }
    }
    ```

    ### (F) POST 핸들러 본문 재작성
    기존 POST 내부 `let mcpClient: Awaited<...>` 블록부터 마지막 `return result.toUIMessageStreamResponse(...)` 까지를 아래로 치환.

    ```typescript
    export async function POST(req: Request) {
      const { messages: uiMessages, modelId } = await req.json();
      const selectedModel = modelId || "gemini-2.5-flash";
      const messages = await convertToModelMessages(uiMessages);

      // D-01/D-02/D-08/D-11: MCP 연결은 모듈 스코프 캐시를 통해.
      let tools: Record<string, unknown> = {};
      try {
        const { tools: cachedTools } = await getOrCreateMcp();
        tools = cachedTools;
      } catch (e) {
        const code = classifyMcpError(e);
        const errMsg = e instanceof Error ? e.message : String(e);
        console.error("[route.ts] mcp init failed:", { code, errMsg });

        // D-05: pre-stream 에러는 구조화된 JSON body + HTTP status로 반환.
        // - mcp_busy  → 503 Service Unavailable
        // - mcp_timeout → 504 Gateway Timeout
        // - mcp_offline → 503 Service Unavailable
        // - unknown    → 500 Internal Server Error
        if (code === "mcp_busy") return makeErrorResponse("mcp_busy", 503);
        if (code === "mcp_timeout") return makeErrorResponse("mcp_timeout", 504);
        if (code === "mcp_offline") return makeErrorResponse("mcp_offline", 503);
        return makeErrorResponse("unknown", 500);
      }

      // D-02 확장: mcpClient는 모듈 스코프 캐시가 소유. 요청 단위 close는 하지 않는다.
      // Phase 1의 closeMcp 패턴(try/catch로 unhandled rejection 방지)은 TTL 만료 시의
      // future work — 현재는 stale client 문제를 cache TTL 5분 + Vercel worker
      // 재시작으로만 해결한다 (SCOPE 문서화: 단일 엔드포인트, 단일 서버).

      const result = streamText({
        model: google(selectedModel),
        system: SYSTEM_PROMPT,
        messages,
        stopWhen: stepCountIs(8),
        ...(Object.keys(tools).length > 0 ? { tools } : {}),
        onFinish: async ({ finishReason }) => {
          console.log("[route.ts] streamText finishReason:", finishReason);
        },
        onError: async ({ error }) => {
          console.error("[route.ts] streamText error:", error);
          // D-02 확장: stream 에러 시 캐시된 client가 오염되었을 가능성.
          // 다음 요청이 새로 연결하도록 cache를 clear.
          cachedClientPromise = null;
          cachedAt = 0;
        },
      });

      return result.toUIMessageStreamResponse({
        consumeSseStream: async ({ stream }) => {
          const reader = stream.getReader();
          try {
            while (true) {
              const { done } = await reader.read();
              if (done) break;
            }
          } finally {
            reader.releaseLock();
          }
        },
        onError: (error) => {
          console.error("[route.ts] toUIMessageStreamResponse error:", error);
          // D-05: mid-stream 에러는 JSON-stringified 한 structured error 문자열을
          // 반환해 client parseChatError()가 pre-stream 경로와 동일하게 파싱한다.
          const code = classifyStreamError(error);
          const structured = JSON.stringify({
            error: { code, message: KOREAN_ERROR_MESSAGES[code] },
          });
          // LAW_API_KEY leak 방지 (Phase 1 redaction 유지).
          return structured.replace(/oc=[^&\s"]+/g, "oc=REDACTED");
        },
      });
    }
    ```

    ### (G) 제거되어야 할 것
    - 기존 `let mcpClient: Awaited<ReturnType<typeof createMCPClient>> | undefined;` 선언
    - 기존 `const closeMcp = async () => { ... }` 블록 (캐시 공유 구조에서 요청별 close가 사라짐)
    - 기존 `try { mcpClient = await createMCPClient(...); tools = await mcpClient.tools(); } catch (e) { ... }` 블록
    - 기존 `onFinish`의 `await closeMcp();`
    - 기존 `onError`의 `await closeMcp();` (RESEARCH §2.2 옵션 1에 의거)

    ### Why 특정 선택들
    - **pending-promise 캐시 패턴**: RESEARCH §2.2가 cache stampede를 경고. Fluid Compute 동시성 하에서 N개 요청이 동시에 miss하면 N개의 createMCPClient가 발사되어 MCP 서버를 503으로 떨어뜨릴 수 있음. promise를 공유하면 단 1회 connect.
    - **mcpClient를 tools와 함께 캐싱**: RESEARCH §2.2 경고 — schema만 캐싱하면 tool.execute가 stale client closure에 bind됨. 옵션 1 채택.
    - **요청별 closeMcp 제거**: mcpClient가 캐시 소유. 요청 스코프에서 close하면 다른 요청의 execute가 깨짐.
    - **onError에서 cache clear**: stream 에러 = client 오염 가능성. 다음 요청이 fresh connect하도록 보수적 cleanup.
    - **HTTP status 코드 배분**: RESEARCH §3.1이 확인함 — client는 body만 사용하므로 status는 관측용. 통상적 매핑(503/504/500) 채택.
    - **mid-stream 에러 JSON-stringify**: RESEARCH §3.2 VERIFIED — onError 반환 문자열이 useChat.error.message로 도달. pre-stream과 동일 포맷을 쓰면 client parseChatError() 한 함수로 양쪽 처리 가능.
    - **`[mcp-cache]` 로그 포맷**: D-04 PoC 절차가 명시. `{hit, age}` 오브젝트로 Vercel 로그에서 grep 용이. 02-03에서 제거.
    - **`[route.ts] mcp retry after 1s` 로그**: D-08 재시도 관측. STRE-06 validation 신호. 02-03에서 제거 여부 판단 (이 로그는 운영 관측에 유용할 수 있어 "remove or keep" 옵션을 02-03에서 열어둠).
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "cachedClientPromise" frontend/src/app/api/chat/route.ts` ≥ 4 (선언 + 3회 이상 접근)
    - `grep -c "CACHE_TTL_MS" frontend/src/app/api/chat/route.ts` ≥ 2
    - `grep -c "getOrCreateMcp" frontend/src/app/api/chat/route.ts` ≥ 2 (정의 + 호출)
    - `grep -c "mcp_timeout" frontend/src/app/api/chat/route.ts` ≥ 3 (Promise.race throw + ErrorCode union + KOREAN_ERROR_MESSAGES)
    - `grep -c "mcp_busy" frontend/src/app/api/chat/route.ts` ≥ 3
    - `grep -c "mcp_offline" frontend/src/app/api/chat/route.ts` ≥ 3
    - `grep -c "stream_timeout" frontend/src/app/api/chat/route.ts` ≥ 3
    - `grep -n "classifyMcpError" frontend/src/app/api/chat/route.ts` → 정의 + ≥ 1회 호출
    - `grep -n "classifyStreamError" frontend/src/app/api/chat/route.ts` → 정의 + ≥ 1회 호출
    - `grep -n "makeErrorResponse" frontend/src/app/api/chat/route.ts` → 정의 + ≥ 3회 호출 (busy/timeout/offline/unknown 중 3개 이상)
    - `grep -n "setTimeout.*5000" frontend/src/app/api/chat/route.ts` → 1 match (Promise.race timeout)
    - `grep -n "setTimeout.*1000" frontend/src/app/api/chat/route.ts` → 1 match (retry 1초 대기)
    - `grep -n "\\[mcp-cache\\]" frontend/src/app/api/chat/route.ts` → 임시 PoC 로그 존재 (D-04; 02-03 Task 01이 이 grep이 0이 되는 것을 검증)
    - `grep -n "\\[route.ts\\] mcp retry after 1s" frontend/src/app/api/chat/route.ts` → 1 match
    - `grep -n "export const maxDuration = 60" frontend/src/app/api/chat/route.ts` → 1 match (변경 없음)
    - `grep -n "D-12" frontend/src/app/api/chat/route.ts` → 1 match (주석 근거)
    - `grep -n "JSON\\.stringify(\\{ error" frontend/src/app/api/chat/route.ts` → ≥ 2 matches (pre-stream makeErrorResponse + mid-stream onError)
    - `grep -n "oc=REDACTED" frontend/src/app/api/chat/route.ts` → 1 match (Phase 1 redaction 보존)
    - `grep -c "try/finally" frontend/src/app/api/chat/route.ts` → 0 (Phase 1 anti-pattern 재도입 금지)
    - 기존 `const closeMcp` 블록 삭제됨: `grep -n "const closeMcp" frontend/src/app/api/chat/route.ts` → 0 matches
    - 기존 `mcpClient.close()`에 의존하는 try/finally 경로 없음
    - `cd frontend && npx tsc --noEmit` exit 0
    - `cd frontend && npm run build` exit 0 (Compiled successfully)
  </acceptance_criteria>
  <done>
    route.ts가 Phase 2 구조화 에러 계약(D-01, D-02, D-05, D-08, D-11, D-12)을 모두 구현하고, D-04 PoC 임시 로그가 삽입되어 있으며, TypeScript/Next.js 빌드가 통과한다. Plan 02-02가 파싱할 JSON body 계약이 확정됨.
  </done>
</task>

</tasks>

<verification>
## Plan-Level Gate (must_haves 증거 수집)

실행 후 다음 grep/command가 전부 성공해야 한다:

```bash
cd frontend
# 1. 타입/빌드
npx tsc --noEmit                                 # exit 0
npm run build                                    # exit 0, "Compiled successfully"

# 2. 구조화 확인
grep -c "cachedClientPromise" src/app/api/chat/route.ts    # ≥ 4
grep -c "CACHE_TTL_MS" src/app/api/chat/route.ts           # ≥ 2
grep -c "getOrCreateMcp" src/app/api/chat/route.ts         # ≥ 2
grep -c "classifyMcpError" src/app/api/chat/route.ts       # ≥ 2
grep -c "classifyStreamError" src/app/api/chat/route.ts    # ≥ 2
grep -c "makeErrorResponse" src/app/api/chat/route.ts      # ≥ 4
grep -c "ErrorCode" src/app/api/chat/route.ts              # ≥ 2

# 3. 에러 code 5개 모두 존재
for c in mcp_timeout mcp_busy mcp_offline stream_timeout unknown; do
  grep -c "$c" src/app/api/chat/route.ts                   # ≥ 2
done

# 4. D-04 PoC 임시 로그
grep -c "\\[mcp-cache\\]" src/app/api/chat/route.ts        # ≥ 2 (hit + miss 로그)
grep -c "mcp retry after 1s" src/app/api/chat/route.ts     # 1

# 5. SYSTEM_PROMPT 완화 (D-14)
grep -c "안녕하세요" src/app/api/chat/route.ts              # 1
grep -c "법률 내용 질문" src/app/api/chat/route.ts           # 1
grep -c "━━━ 절대 규칙 (위반 금지) ━━━" src/app/api/chat/route.ts  # 1
grep -c "도구 검색 결과에 없는 내용을 추가하거나 꾸며내지 마세요" src/app/api/chat/route.ts  # 1

# 6. Phase 1 자산 보존
grep -c "stopWhen: stepCountIs(8)" src/app/api/chat/route.ts    # 1
grep -c "consumeSseStream" src/app/api/chat/route.ts             # 1
grep -c "oc=REDACTED" src/app/api/chat/route.ts                  # 1
grep -c "export const maxDuration = 60" src/app/api/chat/route.ts # 1
grep -c "D-12" src/app/api/chat/route.ts                          # ≥ 1

# 7. Phase 1 anti-pattern 미재도입
grep -c "try {" src/app/api/chat/route.ts | awk '{if ($1 > 6) exit 1}'  # sanity: try 블록이 폭증하지 않음
grep -c "const closeMcp" src/app/api/chat/route.ts               # 0 (요청별 close 제거)
```
</verification>

<success_criteria>
- frontend/src/app/api/chat/route.ts가 Task 02-01-01과 02-01-02의 변경을 모두 반영한 단일 파일 상태로 존재
- `cd frontend && npx tsc --noEmit && npm run build` 둘 다 exit 0
- Plan 02-02가 의존하는 JSON body 계약(`{error:{code,message}}`, 5개 code)이 구현되어 있음
- Plan 02-03이 확인해야 할 PoC 로그 `[mcp-cache]` 와 retry 로그가 삽입되어 있음
- 단일 atomic commit으로 push 가능 (Phase 1 커밋 스타일 준수)
</success_criteria>

<output>
After completion, create `.planning/phases/02-streaming-stability-error-ux/02-01-SUMMARY.md` documenting:
- 실제 변경된 line 수
- 삽입된 PoC 임시 로그 2곳 (02-03이 제거할 hook)
- cache stampede 방어 패턴 채택 근거
- mcpClient 모듈 스코프 공유 선택 근거 (RESEARCH §2.2 옵션 1)
- Deviations (예상: 없음. 있다면 기록)
</output>