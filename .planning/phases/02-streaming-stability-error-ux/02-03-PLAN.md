---
phase: 02-streaming-stability-error-ux
plan: 03
name: uat-smoke-test-and-cleanup
type: execute
wave: 3
depends_on: [02-01, 02-02]
files_modified:
  - frontend/src/app/api/chat/route.ts
  - .planning/PROJECT.md
  - .planning/phases/02-streaming-stability-error-ux/02-VERIFICATION.md
requirements: [STRE-02, STRE-07, STRE-08, STRE-09]
autonomous: false
commit_subject: "docs(02-03): PoC 로그 제거 + maxDuration D-12 근거 + Gemini smoke UAT 결과 기록"
user_setup: []

must_haves:
  truths:
    - "route.ts의 D-04 PoC 임시 로그(`[mcp-cache]`)가 Vercel 프로덕션 로그에서 적어도 한 번의 'miss → hit' 시퀀스가 관찰된 뒤 제거되었다"
    - "route.ts의 D-08 retry 관측 로그(`[route.ts] mcp retry after 1s`) 처리가 명시적으로 결정되어 있다 (제거 or 유지; 결정은 SUMMARY에 기록)"
    - "PROJECT.md Key Decisions 표에 maxDuration = 60 유지 근거 한 줄이 추가되어 있다 (STRE-09 / D-12)"
    - "PROJECT.md 또는 02-VERIFICATION.md에 'Vercel warm-container 캐시 재사용: 확인됨/확인 안 됨' 결과 한 줄이 기록되어 있다 (D-04)"
    - "02-VERIFICATION.md에 Gemini multi-turn smoke test (3연속 질의) 실측 결과가 기록되어 있다 (D-13 / STRE-08)"
    - "02-VERIFICATION.md에 '안녕하세요' 단일턴 smoke test 결과가 기록되어 있고, tool call이 발생하지 않음이 확인되었다 (D-14 / STRE-07)"
    - "02-VERIFICATION.md에 3가지 실패 모드(mcp_offline, mcp_busy, stream_timeout)에 대한 UI 렌더 결과가 기록되어 있다 (STRE-03/04/05)"
    - "Gemini smoke test가 실패했다면 `providerOptions: { google: { thinkingConfig: { thinkingBudget: 0 } } }` 가 route.ts에 적용되어 있다; 통과했다면 현상 유지"
  artifacts:
    - path: ".planning/PROJECT.md"
      provides: "Key Decisions 표에 D-12 maxDuration 유지 근거 및 D-04 warm-container 결과"
      contains: ["maxDuration", "60", "Fluid Compute"]
    - path: ".planning/phases/02-streaming-stability-error-ux/02-VERIFICATION.md"
      provides: "Phase 2 UAT 결과 증거 문서"
      min_lines: 40
      contains: ["D-04", "D-13", "D-14", "mcp_offline", "mcp_busy", "stream_timeout", "thought_signature"]
    - path: "frontend/src/app/api/chat/route.ts"
      provides: "PoC 임시 로그 정리 후 최종 상태"
  key_links:
    - from: ".planning/PROJECT.md Key Decisions 표"
      to: "D-12 근거 라인"
      via: "Markdown 테이블 row"
      pattern: "maxDuration.*60"
    - from: ".planning/phases/02-streaming-stability-error-ux/02-VERIFICATION.md"
      to: "D-04/D-13/D-14 결과"
      via: "섹션 제목 + 결과"
      pattern: "D-04|D-13|D-14"
---

<objective>
Plan 02-01과 02-02의 코드 변경이 프로덕션에서 의도대로 작동하는지 확인하고, 임시 PoC 로그를 정리하며, 문서(PROJECT.md + VERIFICATION.md)에 결과를 기록해 Phase 2를 마무리한다.

Purpose:
- D-04: Vercel warm-container 캐시 재사용을 PoC 로그로 확인한 뒤 임시 로그 제거.
- D-13: Gemini 2.5 Flash 멀티턴 `thought_signature` 안정성을 3연속 법령 질의로 수동 검증. 실패 시 `thinkingBudget: 0` escape hatch 적용.
- D-12: `maxDuration = 60` 유지 결정을 PROJECT.md Key Decisions 표에 명시적으로 기록 (STRE-09 산출물).
- D-14 / D-07 / D-09 / D-10: 서버 프롬프트 완화 + 인라인 에러 UX + 재시도 동작을 실측 검증.
- Phase 2 VERIFICATION.md 작성 — `/gsd-verify-work` 가 바로 승인 가능한 상태로.

Output:
- 정리된 `frontend/src/app/api/chat/route.ts`
- 업데이트된 `.planning/PROJECT.md`
- 신설된 `.planning/phases/02-streaming-stability-error-ux/02-VERIFICATION.md`

Non-autonomous: 프로덕션 배포 + 수동 UAT 절차가 포함되어 `autonomous: false`.
</objective>

<execution_context>
@C:/Users/GLLUGA/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/GLLUGA/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/02-streaming-stability-error-ux/02-CONTEXT.md
@.planning/phases/02-streaming-stability-error-ux/02-RESEARCH.md
@.planning/phases/02-streaming-stability-error-ux/02-VALIDATION.md
@.planning/phases/02-streaming-stability-error-ux/02-01-PLAN.md
@.planning/phases/02-streaming-stability-error-ux/02-02-PLAN.md
@frontend/src/app/api/chat/route.ts
@frontend/CLAUDE.md

<interfaces>
<!-- Plan 02-01에서 삽입된 임시 PoC 로그 위치 -->

route.ts (post Plan 02-01) 안에 삽입된 임시 로그:
```typescript
// Line ~ (getOrCreateMcp 함수 내부)
console.log("[mcp-cache]", { hit: true, age: now - cachedAt });
// ...
console.log("[mcp-cache]", { hit: false, age: now - cachedAt });

// Line ~ (connectMcpWithRetry 함수 내부)
console.log("[route.ts] mcp retry after 1s (503/Max sessions detected)");
```

이 플랜은 `[mcp-cache]` 로그를 완전히 제거하고, `[route.ts] mcp retry after 1s` 는 Claude 판단(유지 또는 제거)한 뒤 SUMMARY에 근거 기록.

<!-- 현재 PROJECT.md Key Decisions 표 구조 -->

From .planning/PROJECT.md (lines 76-86):
```markdown
## Key Decisions

| Decision | Rationale | Outcome |
|----------|-----------|---------|
| 스코프를 Next.js 웹 정비로 한정, Slack 봇 제외 | ... | — Pending |
| Chainlit 레거시 완전 삭제 | ... | — Pending |
| 대화 저장은 localStorage 유지 | ... | — Pending |
| 우선순위: 빈 메시지 버그가 최상단 | ... | — Pending |
| Next.js 16 / AI SDK 6 현행 유지 | ... | — Pending |
| 메시지 파트 추출 로직을 **근본 재설계** (패치가 아닌) | ... | — Pending |
```

새 row 2개 추가: D-12 maxDuration + D-04 warm-container PoC 결과.
</interfaces>
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local/prod Vercel logs → planner/checker | Vercel 대시보드 로그를 수동으로 읽어 PoC 결과를 기록. 출처 검증은 "본인 계정 + 본인 프로젝트" 전제. |
| SYSTEM_PROMPT 변경 → Gemini 출력 | 프롬프트 완화가 법령 hallucination을 유발하지 않는지 5+5 cross-test |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-03-01 | I (Information Disclosure) | VERIFICATION.md에 실측 로그를 붙여넣을 때 LAW_API_KEY 포함 가능 | mitigate | 로그 캡처 시 `oc=...` 부분을 `oc=REDACTED`로 수동 치환. commit 전 `grep -i "oc=" .planning/phases/02-streaming-stability-error-ux/02-VERIFICATION.md` 로 0을 확인. |
| T-02-03-02 | T (Tampering) | SYSTEM_PROMPT 완화로 인한 법령 hallucination — 실제로 법령 질문에서 tool call이 퇴화 | mitigate | UAT에서 "일상 인사" 5회 + "법령 질문" 5회 cross-test. 일상 인사에서 tool call 0, 법령 질문에서 tool call ≥ 1 확인. 실패 시 SYSTEM_PROMPT 재조정 → 02-01에 follow-up commit 또는 새 gap-closure plan. |
| T-02-03-03 | R (Repudiation) | D-13 smoke test 결과가 "대충 통과"로 기록되어 실패 증거가 남지 않음 | mitigate | 각 턴의 응답 텍스트 첫 50자 또는 text part 길이 등 **구체적 증거**를 VERIFICATION.md에 기록. 단순히 "통과" 만 적는 것 금지. |
</threat_model>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 02-03-01: 프로덕션 배포 + D-04 warm-container PoC 관찰</name>
  <files>frontend/src/app/api/chat/route.ts, .planning/phases/02-streaming-stability-error-ux/02-VERIFICATION.md</files>
  <read_first>
    - frontend/src/app/api/chat/route.ts (Plan 02-01/02-02 변경 반영본)
    - .planning/phases/02-streaming-stability-error-ux/02-CONTEXT.md D-04 원문
    - .planning/phases/02-streaming-stability-error-ux/02-RESEARCH.md §2.3 (PoC 절차)
  </read_first>
  <action>
  This is a checkpoint:human-action task. Claude first pushes (or verifies existing push) to trigger Vercel deploy, then pauses for the user to manually validate production behavior. The full UAT script is described in the <what-built> and <how-to-verify> sections below.

  Claude's responsibilities BEFORE pausing for user:
  1. Verify local commits from Plan 02-01 and 02-02 are in HEAD (run `git log --oneline -5`).
  2. If not pushed: instruct user to `git push` (or push if autonomy allows) to trigger Vercel auto-deploy.
  3. Announce the expected Vercel deploy URL and wait for deploy ready state in the Vercel dashboard.
  4. Present the 8-step UAT script from <how-to-verify> to the user and wait for a resume-signal.

  Claude's responsibilities AFTER resume:
  5. Record the user's observations (D-04 PoC result, D-13 Gemini smoke result, D-14 인사+법령 cross-test result, 3-mode UI smoke result) so Task 02-03-02 can conditionally apply `thinkingBudget: 0` and Task 02-03-03 can populate VERIFICATION.md + PROJECT.md with factual values.
  </action>
  <what-built>
    Plan 02-01이 `[mcp-cache]` 관측 로그 2곳과 `[route.ts] mcp retry after 1s` 로그 1곳을 삽입했다. Plan 02-02가 클라이언트 UI를 교체했다. 이제 Vercel 프로덕션에 배포해 실제 serverless 환경에서 cache miss → hit 시퀀스가 발생하는지 확인해야 한다.
  </what-built>
  <how-to-verify>
    1. 로컬에서 변경사항이 main branch에 병합 또는 push 되었는지 확인:
       ```bash
       cd C:/dev/korean-law-bot
       git status
       git log --oneline -5
       ```
       Plan 02-01과 02-02의 commit이 HEAD 근처에 있어야 함.

    2. Vercel 자동 배포 대기 (git push 이후 약 1-2분) 또는 수동 트리거:
       - Vercel 대시보드 → Project `frontend-phi-six-16` → Deployments 탭
       - 가장 최근 commit에 대해 "Ready" 상태 확인
       - URL: `https://frontend-phi-six-16.vercel.app`

    2b. **Vercel Fluid Compute 토글 상태 확인 (D-04 해석에 필수)**:
       - Vercel 대시보드 → Project `frontend-phi-six-16` → Settings → Functions 탭
       - "Fluid Compute" 토글이 enabled/disabled 중 무엇인지 한 눈에 기록한다
       - 이 값을 Task 02-03-03이 02-VERIFICATION.md의 "Environment" 섹션
         `Vercel Fluid Compute: [enabled / disabled / unknown]` 필드에 정확히 한 단어로 채운다
       - Fluid Compute 상태는 D-04 PoC의 warm-container 재사용 해석에 직접 영향을
         주므로 (disabled면 scale-to-zero로 cache miss가 정상), `unknown`으로 남겨두지 말 것

    3. 배포 완료 후 프로덕션에서 **연속으로 3개 질문**을 던진다:
       - 질문 1: "근로기준법 제60조 연차휴가 알려줘"
       - 질문 2 (첫 질문 완료 후 30초 이내): "그럼 제59조는 뭐야?"
       - 질문 3 (두 번째 완료 직후): "제58조도 설명해줘"

       각 답변이 **빈 카드 없이** 텍스트로 렌더링되는지 확인.

    4. Vercel 대시보드 → Project `frontend-phi-six-16` → Functions → `/api/chat` 로그 열기.
       최근 3개 요청의 로그에서 다음 패턴을 찾는다:
       ```
       [mcp-cache] { hit: false, age: ... }    ← 첫 요청 (cold miss)
       [mcp-cache] { hit: true,  age: ... }    ← 둘째 요청 (warm hit)
       [mcp-cache] { hit: true,  age: ... }    ← 셋째 요청 (warm hit)
       ```

       **성공 조건**: 적어도 1회 `hit: false` → 이어서 최소 1회 `hit: true` 시퀀스가 관찰됨.
       **실패 조건**: 3번 모두 `hit: false` (warm container 재사용 없음 → Fluid Compute 미활성화 또는 scale-to-zero).

    5. D-13 Gemini `thought_signature` smoke test (같은 3개 질의로 동시 검증):
       - 세 턴 모두 assistant 응답에 text가 있어야 한다 (빈 답, 500 에러, "이전 대화를 찾을 수 없습니다" 유사 응답은 실패)
       - 실패 시 이 checkpoint를 "REQUIRES_FIX"로 resume하면 Task 02-03-02에서 `thinkingBudget: 0` 적용 경로로 넘어감.

    6. D-14 SYSTEM_PROMPT 완화 smoke test (별도 세션):
       - "안녕하세요" 한 번 → 인사 응답이 나와야 하고, Vercel log에 tool call 관련 로그가 없어야 함. (chat UI상 tool chip이 보이지 않아야 함)
       - "안녕하세요" 5회 반복 시도 (Gemini 확률 동작 대응). 5회 중 0회 tool call.
       - 이어서 "민법 제750조 알려줘" 1회 → tool chip 또는 법령 본문 텍스트가 포함되어야 함 (도구 호출 퇴화 방지 검증).

    7. 3가지 실패 모드 UI 렌더 smoke test (1가지 이상 재현 가능한 것만):
       - (권장) `mcp_offline`: 로컬 .env.local에서 LAW_API_KEY를 잠시 잘못된 값으로 바꾼 뒤 `npm run dev` 로컬 서버에서 질의 → 인라인 에러 배너 + "다시 시도" 버튼 확인. 복원 후 "다시 시도" 클릭 시 정상 응답 확인.
       - 프로덕션에서 재현이 어려우면 (`mcp_busy`, `stream_timeout`은 자연 재현이 희박), 해당 항목은 "재현 불가" 로 기록하고 코드 경로만 로그/tsc로 검증.

    8. 관찰한 결과를 메모장 또는 temporary note에 기록 — 다음 Task 02-03-02에서 VERIFICATION.md에 옮김.

    **결과에 따라 resume signal이 다르다:**
    - 모든 smoke test 통과 → `approved`
    - D-13 실패 (멀티턴 thought_signature 증상) → `gemini-thinking-fix-required`
    - D-04 cache miss만 3회 → `cache-miss-but-continue` (문서 기록만 바뀜)
    - SYSTEM_PROMPT가 법령 질문에서 tool call 퇴화 유발 → `prompt-regression-block` (Task 02-03-02에서 추가 follow-up)
    - 인라인 에러 배너 UI 깨짐 → `ui-regression-block`
  </how-to-verify>
  <resume-signal>
    `approved` | `gemini-thinking-fix-required` | `cache-miss-but-continue` | `prompt-regression-block` | `ui-regression-block`
  </resume-signal>
  <verify>
  Manual UAT gate. Validation signal = user provides one of the resume-signal values after completing the 8-step UAT script in <how-to-verify>. No automated command — the verification is human observation of Vercel Function logs and production UI behavior.
  </verify>
  <done>
  User has resumed with one of the defined resume-signal values, and Claude has captured: (a) D-04 PoC result (관찰됨 / 관찰되지 않음), (b) D-13 Gemini smoke pass/fail per turn, (c) D-14 인사 5회 + 법령 2회 tool-call counts, (d) at least one of the 3 failure modes reproduced in UI (mcp_offline via local .env LAW_API_KEY tweak). These observations are ready for Task 02-03-02 conditional code changes and Task 02-03-03 VERIFICATION.md authoring.
  </done>
</task>

<!-- END TASK 02-03-01 -->

<task type="auto" tdd="false">
  <name>Task 02-03-02: route.ts PoC 로그 정리 + (조건부) Gemini thinkingBudget 적용</name>
  <files>frontend/src/app/api/chat/route.ts</files>
  <read_first>
    - frontend/src/app/api/chat/route.ts (Plan 02-01 반영본)
    - Task 02-03-01의 resume-signal (Claude가 직접 관찰/기록)
    - .planning/phases/02-streaming-stability-error-ux/02-CONTEXT.md D-04 (PoC 로그 제거), D-13 (thinkingBudget escape hatch)
    - .planning/phases/02-streaming-stability-error-ux/02-RESEARCH.md §8.2 (providerOptions 전달 경로)
  </read_first>
  <action>
    ### 필수: `[mcp-cache]` 로그 2곳 완전 제거 (D-04 PoC cleanup)

    `frontend/src/app/api/chat/route.ts` 의 `getOrCreateMcp` 함수에서 다음 두 줄을 **삭제**한다:
    ```typescript
    console.log("[mcp-cache]", { hit: true, age: now - cachedAt });
    console.log("[mcp-cache]", { hit: false, age: now - cachedAt });
    ```
    두 줄이 각각 if/else 분기에 있으므로 delete만 하면 됨. 조건문 구조는 보존 (변수 read는 `age` 변수를 제거하고 필요시 간결화).

    예시 after:
    ```typescript
    async function getOrCreateMcp(): Promise<{ client: MCPClient; tools: Record<string, unknown> }> {
      const now = Date.now();
      if (cachedClientPromise && now - cachedAt < CACHE_TTL_MS) {
        return cachedClientPromise;
      }
      cachedAt = now;
      cachedClientPromise = connectMcpWithRetry();
      try {
        return await cachedClientPromise;
      } catch (e) {
        cachedClientPromise = null;
        cachedAt = 0;
        throw e;
      }
    }
    ```

    ### 선택: `[route.ts] mcp retry after 1s` 로그 결정
    이 로그는 D-08 retry 동작의 운영 관측에 유용할 수 있음. Claude가 다음 중 하나를 선택하고 SUMMARY에 근거 기록:
    - **유지** (권장): 운영 관측 용도로 프로덕션 로그에 계속 남겨 둠. STRE-06 validation 신호가 사후에도 검증 가능.
    - **제거**: D-04 임시 로그와 동일하게 제거하고 코드를 "로그 없이 조용히 동작"으로 바꿈.

    이 플랜의 디폴트 결정은 **유지** (운영 가치 > 로그 노이즈).

    ### 조건부: Gemini thinkingBudget=0 적용
    Task 02-03-01의 resume-signal이 `gemini-thinking-fix-required` 인 경우에만 적용.

    streamText 호출부에 `providerOptions` 필드 추가:
    ```typescript
    const result = streamText({
      model: google(selectedModel),
      system: SYSTEM_PROMPT,
      messages,
      stopWhen: stepCountIs(8),
      ...(Object.keys(tools).length > 0 ? { tools } : {}),
      providerOptions: {
        google: {
          thinkingConfig: { thinkingBudget: 0 },
        },
      },
      onFinish: async ({ finishReason }) => { /* ... */ },
      onError: async ({ error }) => { /* ... */ },
    });
    ```

    resume-signal이 `approved` 인 경우 이 변경은 **적용하지 않음** — Gemini smoke test가 통과했다는 증거이므로.

    ### 조건부: SYSTEM_PROMPT prompt-regression 대응
    Task 02-03-01의 resume-signal이 `prompt-regression-block` 인 경우:
    - 세부 증상(어떤 법령 질문이 tool call을 안 하게 되었는지)에 따라 Claude가 현장에서 판단.
    - 기본 대응: D-14 완화 문장을 살리되 "법률 내용 질문에만 호출합니다" 문장을 더 강조 (볼드/반복). 또는 "법령·시행령·시행규칙·판례·행정규칙 질문은 **반드시** 도구를 호출하세요." 한 줄을 절대 규칙 블록에 추가.
    - 이 경우에도 SUMMARY에 어떤 수정을 했는지 명시.

    ### resume-signal === `ui-regression-block` 분기
    실행을 **중단**하고 코드 변경을 전혀 수행하지 않는다. Claude는 다음을 수행한다:
    - 02-VERIFICATION.md의 "3가지 실패 모드 UI" 섹션(또는 새 "Regression Notes" 섹션)에 관찰된 UI 깨짐 증상을 구체적으로 기록 (어떤 케이스에서 배너가 어떻게 깨지는지, 스크린샷 URL 또는 텍스트 증거).
    - 사용자에게 **새 gap-closure plan 생성 필요**를 안내 (예: `/gsd-plan-phase --gaps` 또는 새 Plan 02-04).
    - Task 02-03-02와 02-03-03은 여기서 실질적 작업을 멈추고 verify가 tsc/build만 확인하도록 한다 (코드 변경 없음).
    - PROJECT.md/route.ts는 건드리지 않는다.

    ### resume-signal === `cache-miss-but-continue` 분기
    코드 변경 없이 문서만 업데이트한다:
    - 02-VERIFICATION.md의 "D-04 Warm-Container PoC" 섹션에 "3회 모두 miss 관찰" 결과와 원인 후보(cold starts, Fluid Compute disabled 등)를 기록 (Task 02-03-03에서 수행).
    - Task 02-03-03이 PROJECT.md Key Decisions 표 D-04 row를 "관찰되지 않음"으로 채운다.
    - route.ts는 정상 cleanup 경로만 밟는다 (`[mcp-cache]` 로그 2곳 제거는 그대로 진행, 추가 정보는 남기지 않음).

    ### resume-signal === `approved` 분기
    위의 필수 cleanup(로그 제거)만 수행하고 조건부 변경(thinkingBudget, prompt fix)은 적용하지 않는다.

    ### 파일 외 변경 금지
    - chat-container.tsx, chat-message.tsx, message-part-renderer.tsx, error-messages.ts 는 이 태스크에서 건드리지 않음.
    - 모든 resume-signal 분기에서 조건부 변경 범위는 오직 route.ts(및 비code 변경은 02-03-03의 VERIFICATION/PROJECT.md 스코프)에만 한정된다.
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "\\[mcp-cache\\]" frontend/src/app/api/chat/route.ts` → **0** (D-04 로그 완전 제거)
    - `grep -c "getOrCreateMcp" frontend/src/app/api/chat/route.ts` → ≥ 2 (함수 자체는 보존)
    - `grep -c "cachedClientPromise" frontend/src/app/api/chat/route.ts` → ≥ 4 (캐시 로직 보존)
    - `grep -c "CACHE_TTL_MS" frontend/src/app/api/chat/route.ts` → ≥ 2
    - `grep -c "classifyMcpError" frontend/src/app/api/chat/route.ts` → ≥ 2
    - `grep -c "makeErrorResponse" frontend/src/app/api/chat/route.ts` → ≥ 4
    - `grep -c "stopWhen: stepCountIs(8)" frontend/src/app/api/chat/route.ts` → 1 (Phase 1 보존)
    - `grep -c "export const maxDuration = 60" frontend/src/app/api/chat/route.ts` → 1
    - resume-signal === `gemini-thinking-fix-required` 일 때:
      - `grep -c "thinkingBudget" frontend/src/app/api/chat/route.ts` → 1
      - `grep -c "providerOptions" frontend/src/app/api/chat/route.ts` → 1
    - resume-signal === `approved` 일 때:
      - `grep -c "thinkingBudget" frontend/src/app/api/chat/route.ts` → 0
      - `grep -c "providerOptions" frontend/src/app/api/chat/route.ts` → 0
    - `cd frontend && npx tsc --noEmit` exit 0
    - `cd frontend && npm run build` exit 0
  </acceptance_criteria>
  <done>
    route.ts에서 D-04 임시 로그가 제거되었고, Gemini smoke test 결과에 따라 `thinkingBudget: 0` 이 조건부 적용되었다. 빌드 통과.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 02-03-03: PROJECT.md Key Decisions 업데이트 + 02-VERIFICATION.md 신설 (D-12, D-04, D-13, D-14, STRE-03/04/05)</name>
  <files>.planning/PROJECT.md, .planning/phases/02-streaming-stability-error-ux/02-VERIFICATION.md</files>
  <read_first>
    - .planning/PROJECT.md (lines 76-86 Key Decisions 표)
    - .planning/phases/02-streaming-stability-error-ux/02-VALIDATION.md (Per-Requirement 표)
    - .planning/phases/02-streaming-stability-error-ux/02-CONTEXT.md (D-04 결과 기록 위치, D-12 근거, D-13 smoke 절차, D-14 smoke 절차)
    - Task 02-03-01에서 Claude가 관찰한 실측 결과 (PoC 결과, Gemini smoke 결과, 인사 smoke 결과, UI smoke 결과)
  </read_first>
  <action>
    ### (A) `.planning/PROJECT.md` Key Decisions 표에 row 2개 추가
    기존 표의 마지막 row(`| 메시지 파트 추출 로직을 **근본 재설계** ... | — Pending |`) **아래**에 다음 2개 row 추가:

    ```markdown
    | `maxDuration = 60` 유지 (STRE-09 / D-12) | Vercel Hobby 플랜 + Fluid Compute 여부와 무관하게 Phase 2 스코프에서는 60초 유지. 평균 Gemini Flash 스트림은 30초 내 완료되고, 타임아웃 시 `stream_timeout` 인라인 에러 UX로 방어. 상향(300s)은 별도 인프라 결정 항목. | Decided (Phase 2) |
    | MCP warm-container 캐시 재사용 확인 (STRE-02 / D-04) | 5분 TTL 모듈 스코프 캐시 + pending-promise stampede 방어. 프로덕션 PoC에서 `[mcp-cache]` miss→hit 시퀀스 **{관찰됨 / 관찰되지 않음}** (Task 02-03-01 실측). | Decided (Phase 2) |
    ```

    `{관찰됨 / 관찰되지 않음}` placeholder는 Task 02-03-01의 실측 결과에 따라 정확한 한 단어로 치환한다:
    - D-04 PoC에서 최소 1회 hit 관찰 → "관찰됨"
    - 3회 모두 miss → "관찰되지 않음"

    ### (B) `.planning/phases/02-streaming-stability-error-ux/02-VERIFICATION.md` 신설
    아래 구조로 신규 파일 작성. 실측 결과는 Task 02-03-01에서 Claude가 직접 관찰한 값으로 채운다. `[기록]` 플레이스홀더는 모두 실제 값으로 대체.

    ```markdown
    ---
    phase: 02-streaming-stability-error-ux
    created: 2026-04-13
    status: verified | verified-with-exceptions
    ---

    # Phase 02 — Verification Log

    Phase 2 Success Criteria 9개 + D-04/D-13/D-14 PoC/smoke 실측 결과.

    ## Environment

    - **Production URL:** https://frontend-phi-six-16.vercel.app
    - **Deploy commit:** `[SHA of deploy]`
    - **Date:** `[YYYY-MM-DD HH:MM KST]`
    - **Vercel Fluid Compute:** `[enabled / disabled / unknown]` (대시보드 확인 결과)

    ## D-04 Warm-Container PoC (STRE-02)

    **Procedure:** 연속 3개 질의 후 Vercel Function logs에서 `[mcp-cache]` 패턴 확인.

    **Result:** `[관찰됨 / 관찰되지 않음]`

    **Log excerpts (Vercel Functions → /api/chat):**
    ```
    [첫 요청 로그 한 줄 — hit:false]
    [둘째 요청 로그 한 줄 — hit:true 또는 false]
    [셋째 요청 로그 한 줄 — hit:true 또는 false]
    ```

    **Conclusion:** `[hit 관찰 시] 모듈 스코프 캐시가 warm container에서 재사용됨. STRE-02 충족. / [hit 미관찰 시] warm container 재사용이 관찰되지 않음. 원인 후보: cold starts 간격, Fluid Compute 비활성화, concurrent isolation. 본 Phase 스코프에서는 기록만 남기고 향후 재평가.`

    ## D-13 Gemini Multi-turn `thought_signature` Smoke (STRE-08)

    **Procedure:** 3연속 법령 질의
    1. "근로기준법 제60조 연차휴가 알려줘"
    2. "그럼 제59조는 뭐야?"
    3. "제58조도 설명해줘"

    **Result per turn:**
    | Turn | Question | Assistant response first 80 chars | Has `text` part? | Has `tool-*` part? | Pass? |
    |------|----------|-----------------------------------|------------------|---------------------|-------|
    | 1 | 근로기준법 제60조 | `[첫 답변 첫 80자]` | yes / no | yes / no | ✓ / ✗ |
    | 2 | 제59조는? | `[둘째 답변 첫 80자]` | yes / no | yes / no | ✓ / ✗ |
    | 3 | 제58조도 | `[셋째 답변 첫 80자]` | yes / no | yes / no | ✓ / ✗ |

    **Overall:** `[PASS / FAIL]`

    **Action taken:** `[PASS 시] 현재 route.ts 그대로 유지. / [FAIL 시] providerOptions.google.thinkingConfig.thinkingBudget = 0 적용됨 (Task 02-03-02).`

    ## D-14 SYSTEM_PROMPT Relaxation Smoke (STRE-07)

    **Procedure A — 일상 인사 5회:**
    | Attempt | Prompt | tool call 발생? |
    |---------|--------|-----------------|
    | 1 | "안녕하세요" | yes / no |
    | 2 | "안녕하세요" | yes / no |
    | 3 | "고마워요" | yes / no |
    | 4 | "뭐 할 수 있어?" | yes / no |
    | 5 | "이름이 뭐야?" | yes / no |

    **Procedure B — 법령 질문 regression check:**
    | Attempt | Prompt | tool call 발생? | 답변에 법령 본문 포함? |
    |---------|--------|-----------------|----------------------|
    | 1 | "민법 제750조 알려줘" | yes / no | yes / no |
    | 2 | "근로기준법 연차 규정" | yes / no | yes / no |

    **Overall:** `[PASS / FAIL]`. 기준: 인사 5회 모두 tool call 없음 + 법령 2회 모두 tool call ≥ 1.

    ## 3가지 실패 모드 UI (STRE-03, STRE-04, STRE-05)

    | Failure mode | Reproduction | Observed UI | Inline banner? | Retry button? | Pass? |
    |--------------|--------------|-------------|----------------|---------------|-------|
    | `mcp_offline` | 로컬 .env.local에 잘못된 LAW_API_KEY + /test-sidebar 질의 | `[관찰 내용]` | yes / no | yes / no | ✓ / ✗ |
    | `mcp_busy` | (재현 불가 시) code path review + TypeScript gate | N/A | `parseChatError('{"error":{"code":"mcp_busy"...}}')` → KOREAN_MESSAGES.mcp_busy 반환 확인 | - | ✓ (code path only) |
    | `stream_timeout` | (재현 불가 시) code path review | N/A | `parseChatError('{"error":{"code":"stream_timeout"...}}')` → KOREAN_MESSAGES.stream_timeout 반환 확인 | - | ✓ (code path only) |

    **Retry 동작 smoke:**
    - Pre-stream 에러(mcp_offline) 재현 후 "다시 시도" 클릭 → sendMessage fallback 경로로 재전송 → 정상 응답 확인: `[관찰 내용]`

    ## maxDuration = 60 유지 근거 (STRE-09 / D-12)

    PROJECT.md Key Decisions 표에 추가됨. 요지:
    - Vercel Hobby 플랜 + Phase 2 스코프에서 상향 불필요.
    - 평균 스트림 30초 내 완료, 타임아웃은 `stream_timeout` 인라인 UX로 방어.
    - 상향(300s)은 별도 인프라 결정 항목으로 지연.

    ## Requirements Coverage

    | ID | Addressed by | Evidence | Status |
    |----|--------------|----------|--------|
    | STRE-01 | Plan 02-01 (Promise.race 5s + degraded), Plan 02-02 (mcp_offline 메시지) | route.ts line refs + UI smoke 결과 | ✓ / ✗ |
    | STRE-02 | Plan 02-01 (cachedClientPromise + TTL) | D-04 PoC 결과 위 | ✓ / ✗ |
    | STRE-03 | Plan 02-02 (ChatMessage inline banner) | UI smoke 결과 위 | ✓ / ✗ |
    | STRE-04 | Plan 02-01 (JSON body) + Plan 02-02 (parseChatError) | UI smoke + tsc 통과 | ✓ / ✗ |
    | STRE-05 | Plan 02-02 (handleRetry + regenerate/sendMessage fallback) | Retry smoke 결과 | ✓ / ✗ |
    | STRE-06 | Plan 02-01 (connectMcpWithRetry) | route.ts code path review (실측 재현 어려움) | ✓ (code-only) |
    | STRE-07 | Plan 02-01 (D-14 SYSTEM_PROMPT) | D-14 smoke 결과 위 | ✓ / ✗ |
    | STRE-08 | Plan 02-01 (조건부 thinkingBudget) | D-13 smoke 결과 위 | ✓ / ✗ |
    | STRE-09 | Plan 02-03 (PROJECT.md row 추가) | PROJECT.md diff | ✓ |

    ## Overall Phase 2 Status

    **`[verified / verified-with-exceptions / rejected]`**

    Notes: `[만약 exceptions가 있다면 여기에 기록 — 예: mcp_busy/stream_timeout UI는 재현 불가로 code path only 검증]`
    ```

    ### (C) LAW_API_KEY 누설 방지 grep
    VERIFICATION.md 작성 후, 커밋 전에 다음 명령으로 oc= 누설을 확인:
    ```bash
    grep -i "oc=" .planning/phases/02-streaming-stability-error-ux/02-VERIFICATION.md
    ```
    결과에 키 값이 보이면 해당 부분을 `oc=REDACTED` 로 치환한 뒤 다시 저장.

    ### 파일 외 변경 금지
    - route.ts 는 이 태스크에서 건드리지 않음 (Task 02-03-02에서 정리 완료)
    - 다른 코드 파일도 건드리지 않음
  </action>
  <verify>
    <automated>grep -q "maxDuration" .planning/PROJECT.md && grep -q "warm-container" .planning/PROJECT.md && test -f .planning/phases/02-streaming-stability-error-ux/02-VERIFICATION.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "maxDuration" .planning/PROJECT.md` → ≥ 1 (Key Decisions 표에 추가됨)
    - `grep -c "warm-container" .planning/PROJECT.md` → ≥ 1 (D-04 결과 row)
    - `grep -c "STRE-09" .planning/PROJECT.md` → ≥ 1
    - `grep -c "D-12" .planning/PROJECT.md` → ≥ 1
    - `grep -c "Decided (Phase 2)" .planning/PROJECT.md` → ≥ 2 (새 row 2개)
    - `test -f .planning/phases/02-streaming-stability-error-ux/02-VERIFICATION.md` → OK
    - VERIFICATION.md의 모든 `[기록]`/`[hit 관찰 시]`/`[PASS / FAIL]` 플레이스홀더가 실제 값으로 치환됨 (수동 검토): `grep -c "\\[기록\\]\\|\\[PASS / FAIL\\]\\|\\[관찰 내용\\]" .planning/phases/02-streaming-stability-error-ux/02-VERIFICATION.md` → 0
    - `grep -c "D-04" .planning/phases/02-streaming-stability-error-ux/02-VERIFICATION.md` → ≥ 1
    - `grep -c "D-13" .planning/phases/02-streaming-stability-error-ux/02-VERIFICATION.md` → ≥ 1
    - `grep -c "D-14" .planning/phases/02-streaming-stability-error-ux/02-VERIFICATION.md` → ≥ 1
    - `grep -c "mcp_offline" .planning/phases/02-streaming-stability-error-ux/02-VERIFICATION.md` → ≥ 1
    - `grep -c "thought_signature" .planning/phases/02-streaming-stability-error-ux/02-VERIFICATION.md` → ≥ 1
    - `grep -c "STRE-0" .planning/phases/02-streaming-stability-error-ux/02-VERIFICATION.md` → ≥ 9 (STRE-01~09 모두 참조)
    - LAW_API_KEY 누설 없음: `grep -i "oc=[a-zA-Z0-9]" .planning/phases/02-streaming-stability-error-ux/02-VERIFICATION.md` → 0 (REDACTED만 허용)
  </acceptance_criteria>
  <done>
    PROJECT.md에 D-12 + D-04 결과 2개 row가 추가되었고, 02-VERIFICATION.md가 실측 증거로 채워져 있다. Phase 2 전체 9개 STRE requirement에 대한 증거가 수집되었다.
  </done>
</task>

</tasks>

<verification>
## Plan-Level Gate

```bash
# 1. route.ts D-04 PoC cleanup
grep -c "\\[mcp-cache\\]" frontend/src/app/api/chat/route.ts            # 0
grep -c "getOrCreateMcp" frontend/src/app/api/chat/route.ts             # ≥ 2 (함수 보존)
grep -c "cachedClientPromise" frontend/src/app/api/chat/route.ts        # ≥ 4

# 2. 빌드
cd frontend && npx tsc --noEmit                                          # exit 0
cd frontend && npm run build                                             # exit 0

# 3. PROJECT.md 업데이트
grep -c "maxDuration" .planning/PROJECT.md                               # ≥ 1
grep -c "warm-container" .planning/PROJECT.md                            # ≥ 1
grep -c "Decided (Phase 2)" .planning/PROJECT.md                         # ≥ 2

# 4. VERIFICATION.md 존재 및 내용
test -f .planning/phases/02-streaming-stability-error-ux/02-VERIFICATION.md
grep -c "D-04\\|D-13\\|D-14" .planning/phases/02-streaming-stability-error-ux/02-VERIFICATION.md  # ≥ 3
grep -c "STRE-0" .planning/phases/02-streaming-stability-error-ux/02-VERIFICATION.md  # ≥ 9

# 5. LAW_API_KEY 누설 없음
grep -ri "oc=[a-zA-Z0-9]" .planning/phases/02-streaming-stability-error-ux/  # 0 real leaks
```
</verification>

<success_criteria>
- Task 02-03-01 resume-signal 이 `approved` 또는 `gemini-thinking-fix-required` 로 해결
- 모든 Phase 2 Success Criteria 9개에 대한 증거가 VERIFICATION.md에 명시
- `maxDuration = 60` 유지 근거가 PROJECT.md Key Decisions 표에 기록됨 (STRE-09 완결)
- D-04 warm-container PoC 임시 로그가 제거되고, 결과가 문서화됨 (STRE-02 완결)
- D-13 Gemini multi-turn smoke test 결과가 기록되고, 필요 시 `thinkingBudget: 0` 적용됨 (STRE-08 완결)
- D-14 일상 인사 + 법령 질문 cross-test 결과가 기록됨 (STRE-07 완결)
- 3개 실패 모드 중 최소 1개(mcp_offline)의 UI 렌더 증거 + 나머지 2개는 code path review 로 기록 (STRE-03/04/05 완결)
- `/gsd-verify-work` 가 바로 승인 가능한 상태
</success_criteria>

<output>
After completion, create `.planning/phases/02-streaming-stability-error-ux/02-03-SUMMARY.md` documenting:
- Task 02-03-01 resume-signal
- D-04 PoC 결과 (관찰됨/관찰되지 않음)
- D-13 Gemini smoke pass/fail + 적용 여부
- D-14 인사 + 법령 regression test 결과
- retry 로그 유지 vs 제거 결정 및 근거
- VERIFICATION.md 작성 완료 (line count)
- PROJECT.md Key Decisions 2 row 추가 완료
- Phase 2 전체 상태 (verified / verified-with-exceptions / rejected)
</output>