---
phase: 02-streaming-stability-error-ux
plan: 02
name: client-error-ux-inline-retry
type: execute
wave: 2
depends_on: [02-01]
files_modified:
  - frontend/src/lib/error-messages.ts
  - frontend/src/components/chat/chat-message.tsx
  - frontend/src/components/chat/message-part-renderer.tsx
  - frontend/src/components/chat/chat-container.tsx
requirements: [STRE-01, STRE-03, STRE-04, STRE-05]
autonomous: true
commit_subject: "feat(02-02): 인라인 에러 배너 + 다시 시도 버튼 + error-messages.ts 매핑"
user_setup: []

must_haves:
  truths:
    - "chat-container.tsx의 기존 global error block(라인 ~190-203, `error && (...)` + '혼잡' 문자열 기반 분기)이 완전히 제거되었다"
    - "frontend/src/lib/error-messages.ts가 새로 생성되어 ErrorCode union + KOREAN_MESSAGES 테이블 + parseChatError 함수를 export한다"
    - "parseChatError는 JSON `{error:{code,message}}` 파싱과 legacy raw-string fallback 두 경로를 모두 처리한다"
    - "ChatMessage가 `error?: ParsedError`, `onRetry?: () => void`, `isRetryDisabled?: boolean` 세 prop을 추가로 받아 인라인 배너를 렌더한다"
    - "ChatMessage의 인라인 에러 배너는 rounded border + `bg-destructive/5` 배경 + '다시 시도' 버튼 UI를 갖는다"
    - "MessagePartRenderer는 error/onRetry/isRetryDisabled 3개 prop을 ChatMessage에 pass-through한다"
    - "ChatContainer가 useChat에서 regenerate, clearError를 꺼내 onRetry에 wire한다"
    - "retry handler는 마지막 메시지가 assistant이면 regenerate(), user이면 sendMessage로 fallback한다 (pre-stream 에러 대응)"
    - "retry 버튼은 status === 'streaming' || status === 'submitted'일 때 disabled 상태"
    - "ChatContainer는 마지막 메시지가 user인 pre-stream 에러 케이스에서 standalone error bubble을 user 메시지 아래에 렌더한다"
    - "mcp_offline 또는 mcp_timeout 메시지에는 '[⚠️ 미확인 답변]' 프리픽스 또는 동일 의미의 표식이 포함된다 (D-03)"
  artifacts:
    - path: "frontend/src/lib/error-messages.ts"
      provides: "ErrorCode union + KOREAN_MESSAGES + parseChatError + ParsedError type"
      exports: ["ErrorCode", "ParsedError", "parseChatError", "KOREAN_MESSAGES"]
      min_lines: 50
    - path: "frontend/src/components/chat/chat-message.tsx"
      provides: "ChatMessage with error prop + inline error banner + retry button"
      contains: ["error?:", "onRetry?:", "isRetryDisabled", "다시 시도", "bg-destructive/5"]
    - path: "frontend/src/components/chat/message-part-renderer.tsx"
      provides: "pass-through of error/onRetry/isRetryDisabled"
      contains: ["error?:", "onRetry?:", "isRetryDisabled"]
    - path: "frontend/src/components/chat/chat-container.tsx"
      provides: "error routing from useChat to last assistant bubble + pre-stream fallback"
      contains: ["regenerate", "clearError", "parseChatError", "handleRetry"]
  key_links:
    - from: "frontend/src/components/chat/chat-container.tsx (handleRetry)"
      to: "useChat().regenerate() | sendMessage()"
      via: "분기: 마지막 메시지 role에 따라"
      pattern: "regenerate\\(\\)|sendMessage\\("
    - from: "frontend/src/components/chat/chat-container.tsx (render)"
      to: "MessagePartRenderer error prop"
      via: "마지막 assistant 메시지에만 parseChatError(error) 주입"
      pattern: "error=\\{"
    - from: "frontend/src/components/chat/message-part-renderer.tsx"
      to: "ChatMessage error prop"
      via: "pass-through"
      pattern: "error=\\{error\\}"
    - from: "frontend/src/lib/error-messages.ts (parseChatError)"
      to: "JSON.parse / raw string matching"
      via: "try/catch + legacy fallback"
      pattern: "JSON\\.parse|ECONNREFUSED|503"
---

<objective>
클라이언트 측 에러 UX를 Plan 02-01의 JSON 계약에 맞춰 재구성한다. 기존 global error block을 완전히 제거하고, 실패한 assistant bubble 내부에 인라인 배너 + 다시 시도 버튼을 렌더링한다.

Purpose:
- D-03, D-06, D-07, D-09, D-10 구현. Plan 02-01이 반환하는 `{error:{code,message}}` 구조와 mid-stream SSE error chunk 문자열 양쪽을 하나의 parser(parseChatError)로 통합.
- Pre-stream 에러(assistant 메시지가 아직 없는 상태)와 mid-stream 에러(assistant bubble이 존재) 두 DOM 경로를 구분해 처리 (RESEARCH §3.3 / §4.3 / Q5 결정).
- regenerate()가 pre-stream 케이스에 동작하지 않을 가능성(RESEARCH §5.2, Q3)을 대비해 sendMessage fallback을 미리 구현해 UAT 실측 필요를 제거.

Output:
- 신규 `frontend/src/lib/error-messages.ts` (Plan 02-01 JSON 계약 mirror)
- 수정 `frontend/src/components/chat/chat-message.tsx` (error prop + 인라인 배너)
- 수정 `frontend/src/components/chat/message-part-renderer.tsx` (prop pass-through)
- 수정 `frontend/src/components/chat/chat-container.tsx` (global block 제거 + 라우팅)
</objective>

<execution_context>
@C:/Users/GLLUGA/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/GLLUGA/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/02-streaming-stability-error-ux/02-CONTEXT.md
@.planning/phases/02-streaming-stability-error-ux/02-RESEARCH.md
@.planning/phases/02-streaming-stability-error-ux/02-VALIDATION.md
@.planning/phases/02-streaming-stability-error-ux/02-01-PLAN.md
@frontend/src/components/chat/chat-container.tsx
@frontend/src/components/chat/chat-message.tsx
@frontend/src/components/chat/message-part-renderer.tsx
@frontend/src/lib/ui-message-parts.ts
@frontend/src/app/api/chat/route.ts
@frontend/CLAUDE.md
@frontend/AGENTS.md

<interfaces>
<!-- Plan 02-01이 반환하는 JSON 계약 (이 플랜이 파싱 대상으로 삼음) -->

Server error body contract (from Plan 02-01):
```typescript
type ErrorCode = "mcp_timeout" | "mcp_busy" | "mcp_offline" | "stream_timeout" | "unknown";

// Pre-stream path (createMCPClient 실패 등): HTTP status 503/504/500 + JSON body
// Mid-stream path (toUIMessageStreamResponse onError 반환): SSE error chunk의 errorText로 흐름
// 두 경로 모두 body/text 는 동일 형식:
//   '{"error":{"code":"mcp_busy","message":"..."}}'
```

Client receive path (from RESEARCH §3 VERIFIED):
```typescript
// Pre-stream: useChat.error.message = responseBody 전체 문자열 (HttpChatTransport가 new Error(body) 생성)
// Mid-stream: useChat.error.message = onError 반환 문자열 전체 (processUIMessageStream이 new Error(errorText) 생성)
// → 양쪽 모두 "문자열" 한 줄에 JSON 구조가 들어 있으므로, parseChatError는 try JSON.parse 단일 경로로 통합 가능.
```

useChat exports (from frontend/node_modules/@ai-sdk/react/dist/index.d.ts VERIFIED):
```typescript
type UseChatHelpers<UI_MESSAGE extends UIMessage> = {
  readonly id: string;
  setMessages: ...;
  error: Error | undefined;
} & Pick<AbstractChat<UI_MESSAGE>, 'sendMessage' | 'regenerate' | 'stop' | 'resumeStream'
  | 'addToolResult' | 'addToolOutput' | 'addToolApprovalResponse'
  | 'status' | 'messages' | 'clearError'>;

// regenerate 시그너처:
regenerate: ({ messageId, ...options }?: {
  messageId?: string;
} & ChatRequestOptions) => Promise<void>;
// messageId 생략 시 마지막 assistant 메시지를 타겟 (RESEARCH §5.1 VERIFIED).
```

From frontend/src/components/chat/chat-message.tsx (current):
```typescript
interface ChatMessageProps {
  role: "user" | "assistant";
  content: string;
  id?: string;
  isFavorite?: boolean;
  onToggleFavorite?: (id: string) => void;
}
// content !== "검색 중..." 체크가 로딩 UI 분기에 사용됨 (Phase 3에서 교체 예정 — 건드리지 않음).
```

From frontend/src/components/chat/message-part-renderer.tsx (current):
```typescript
interface MessagePartRendererProps {
  message: ExtractableMessage;
  isFavorite?: boolean;
  onToggleFavorite?: (id: string) => void;
}
// Phase 1에서 생성됨. 이번 플랜이 error/onRetry/isRetryDisabled 3개 prop을 추가하고 ChatMessage에 pass-through.
```

From frontend/src/lib/ui-message-parts.ts (current):
```typescript
// ExtractableMessage = UIMessage | LegacyMessage
// extractAssistantText(msg): string
// MessagePartRenderer와 chat-container가 모두 이 모듈만 import.
```
</interfaces>
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| server(/api/chat) → client useChat.error.message | 서버가 JSON 또는 raw 문자열로 에러를 전달. client는 신뢰하지만 렌더 전 parse 필요 |
| client DOM → user | 에러 메시지가 HTML escape 없이 렌더될 경우 XSS 가능성 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-02-01 | I (Information Disclosure) | parseChatError legacy fallback이 raw server error.message를 그대로 렌더 | mitigate | legacy fallback은 raw 문자열을 **렌더하지 않고**, 코드 기반 분기(`includes("503")` 등) 후 미리 정의된 KOREAN_MESSAGES 값을 반환한다. raw string을 UI에 절대 노출하지 않음. |
| T-02-02-02 | T (Tampering/XSS) | ChatMessage 인라인 배너가 message 문자열을 렌더 | mitigate | message는 KOREAN_MESSAGES의 미리 정의된 한국어 상수에서만 나온다. 서버가 조작한 message는 legacy fallback에서도 무시되고 상수로 대체. React 기본 escape 유지 (innerHTML 사용 금지). |
| T-02-02-03 | D (Denial of Service/UX) | 재시도 버튼 연타 → 동시 regenerate 호출 | mitigate | D-10: `isRetryDisabled = status === 'streaming' \|\| status === 'submitted'` 로 중복 방지. clearError() → regenerate() 순서는 React batch로 한 tick에 처리. |
| T-02-02-04 | I | console.error에 raw error.message 그대로 로그 → 브라우저 devtools에 MCP URL/key 노출 | accept | 브라우저 client 로그. 이미 서버 /oc=REDACTED redaction 으로 1차 방어. devtools 접근자는 인증된 사용자 본인(NextAuth 통과)이므로 2차 위협 모델 밖. |
| T-02-02-05 | E (Elevation) | pre-stream fallback에서 sendMessage로 last user message 재전송 시, user message를 filter하지 못하면 중복 작성 | mitigate | handleRetry 분기에서 "마지막 메시지가 user일 때만" sendMessage 재전송. messages.findLast로 명시적으로 last user를 취득. |
</threat_model>

<tasks>

<task type="auto" tdd="false">
  <name>Task 02-02-01: error-messages.ts 신설 (D-06, ErrorCode mirror of server contract)</name>
  <files>frontend/src/lib/error-messages.ts</files>
  <read_first>
    - frontend/src/lib/ui-message-parts.ts (Phase 1에서 확립된 lib/ 파일 스타일)
    - frontend/src/app/api/chat/route.ts (Plan 02-01의 KOREAN_ERROR_MESSAGES 값)
    - .planning/phases/02-streaming-stability-error-ux/02-RESEARCH.md §3.3 (parseChatError 설계 + legacy fallback 패턴)
    - .planning/phases/02-streaming-stability-error-ux/02-CONTEXT.md D-06 (한국어 메시지 원문)
  </read_first>
  <action>
    새 파일 `frontend/src/lib/error-messages.ts` 를 생성한다. 내용은 아래 **그대로** 복사한다 (Claude 재량으로 미세 조정 가능하지만, export 이름/ErrorCode union/KOREAN_MESSAGES 키-값은 고정):

    ```typescript
    /**
     * Chat error parsing + Korean message mapping.
     *
     * Server (frontend/src/app/api/chat/route.ts, Plan 02-01) returns error
     * info in two paths that share a single string format:
     *
     *   pre-stream  (HTTP 4xx/5xx body) : '{"error":{"code":"...","message":"..."}}'
     *   mid-stream  (SSE error chunk)   : '{"error":{"code":"...","message":"..."}}'
     *
     * useChat captures both into `error.message` as a string. parseChatError
     * first tries JSON.parse, and falls back to raw substring matching for
     * robustness across version drift.
     *
     * ─── 메시지 source-of-truth 규약 (Phase 2) ───────────────────
     * **이 파일(KOREAN_MESSAGES)이 사용자에게 렌더되는 한국어 문자열의 유일한
     * canonical source다.** 서버 route.ts의 KOREAN_ERROR_MESSAGES는 내부
     * debug log 용도이며, parseChatError는 서버가 보낸 body의 `code` 필드만
     * 사용하고 `message` 필드는 완전히 무시한다(아래 primary 경로 참조).
     * 서버/클라이언트 문자열이 drift해도 UI는 항상 이 파일의 값만 본다.
     * 문구 변경 시 이 파일만 수정하면 되고, 서버 상수와 동기화하지 말 것.
     * 규약 변경 시 frontend/src/app/api/chat/route.ts의 KOREAN_ERROR_MESSAGES
     * 위 주석도 함께 갱신할 것.
     * ───────────────────────────────────────────────────────────
     *
     * Requirements: STRE-04, D-06.
     */

    export type ErrorCode =
      | "mcp_timeout"
      | "mcp_busy"
      | "mcp_offline"
      | "stream_timeout"
      | "unknown";

    export interface ParsedError {
      code: ErrorCode;
      message: string; // Korean, ready to render
    }

    /**
     * D-06 한국어 매핑 + D-03 degraded mode "[⚠️ 미확인 답변]" 프리픽스.
     * mcp_timeout / mcp_offline 은 degraded mode 프리픽스를 포함.
     */
    export const KOREAN_MESSAGES: Record<ErrorCode, string> = {
      mcp_timeout:
        "[⚠️ 미확인 답변] 법령 검색 서버 연결이 지연되어 일반 답변만 드릴 수 있습니다.",
      mcp_busy:
        "법령 검색 서버가 현재 혼잡합니다. 잠시 후 다시 시도해주세요.",
      mcp_offline:
        "[⚠️ 미확인 답변] 법령 검색 서버에 연결할 수 없어 일반 답변만 드릴 수 있습니다.",
      stream_timeout:
        "응답 생성 시간이 초과되었습니다. 질문을 더 간단히 해보세요.",
      unknown:
        "알 수 없는 오류가 발생했습니다. 새로고침 후 다시 시도해주세요.",
    };

    /**
     * Parse a useChat error into a structured ParsedError.
     *
     * 1) 서버가 JSON `{error:{code,message}}` 형태로 보냈으면 code 기반 매핑 (primary).
     * 2) Legacy raw-string fallback: 503/"Max sessions"/ECONNREFUSED/ENOTFOUND 같은
     *    단서로 분기. 절대 raw 문자열을 UI에 노출하지 않고 KOREAN_MESSAGES로 치환.
     * 3) 매칭 실패 → "unknown".
     */
    export function parseChatError(err: Error | undefined): ParsedError {
      if (!err) return { code: "unknown", message: KOREAN_MESSAGES.unknown };
      const raw = err.message ?? "";

      // Primary: structured JSON body from Plan 02-01.
      try {
        const parsed = JSON.parse(raw) as unknown;
        if (
          parsed &&
          typeof parsed === "object" &&
          "error" in parsed &&
          parsed.error &&
          typeof parsed.error === "object" &&
          "code" in parsed.error
        ) {
          const code = (parsed.error as { code: unknown }).code;
          if (typeof code === "string" && code in KOREAN_MESSAGES) {
            const typed = code as ErrorCode;
            return { code: typed, message: KOREAN_MESSAGES[typed] };
          }
        }
      } catch {
        // fall through to raw-string matching
      }

      // Legacy/raw fallback.
      if (raw.includes("503") || /Max sessions/i.test(raw) || raw.includes("429")) {
        return { code: "mcp_busy", message: KOREAN_MESSAGES.mcp_busy };
      }
      if (
        raw.includes("ECONNREFUSED") ||
        raw.includes("ENOTFOUND") ||
        raw.includes("fetch failed")
      ) {
        return { code: "mcp_offline", message: KOREAN_MESSAGES.mcp_offline };
      }
      if (/aborted|timeout/i.test(raw)) {
        return { code: "stream_timeout", message: KOREAN_MESSAGES.stream_timeout };
      }

      return { code: "unknown", message: KOREAN_MESSAGES.unknown };
    }
    ```

    파일 외 변경 없음. 다른 파일이 이 모듈을 import하는 것은 Task 02-02-04에서 수행.
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - 파일 존재: `ls frontend/src/lib/error-messages.ts` → OK
    - `grep -c "export type ErrorCode" frontend/src/lib/error-messages.ts` → 1
    - `grep -c "export interface ParsedError" frontend/src/lib/error-messages.ts` → 1
    - `grep -c "export const KOREAN_MESSAGES" frontend/src/lib/error-messages.ts` → 1
    - `grep -c "export function parseChatError" frontend/src/lib/error-messages.ts` → 1
    - 5개 ErrorCode 값이 KOREAN_MESSAGES에 모두 존재: `grep -c "mcp_timeout:" frontend/src/lib/error-messages.ts` ≥ 1, 동일하게 `mcp_busy:`, `mcp_offline:`, `stream_timeout:`, `unknown:` 각 ≥ 1
    - `grep -c "\\[⚠️ 미확인 답변\\]" frontend/src/lib/error-messages.ts` ≥ 2 (mcp_timeout + mcp_offline)
    - `grep -c "JSON.parse" frontend/src/lib/error-messages.ts` ≥ 1
    - `grep -c "ECONNREFUSED" frontend/src/lib/error-messages.ts` ≥ 1
    - `grep -c "Max sessions" frontend/src/lib/error-messages.ts` ≥ 1
    - `cd frontend && npx tsc --noEmit` exit 0
  </acceptance_criteria>
  <done>error-messages.ts가 JSON parser + legacy fallback + 5개 ErrorCode의 한국어 매핑을 제공한다. TypeScript check 통과.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 02-02-02: ChatMessage에 error/onRetry/isRetryDisabled prop 추가 + 인라인 에러 배너 (D-07)</name>
  <files>frontend/src/components/chat/chat-message.tsx</files>
  <read_first>
    - frontend/src/components/chat/chat-message.tsx (전체, 현재 82 lines)
    - frontend/src/lib/error-messages.ts (Task 02-02-01 결과물)
    - .planning/phases/02-streaming-stability-error-ux/02-CONTEXT.md D-07
    - .planning/phases/02-streaming-stability-error-ux/02-RESEARCH.md §4 (리팩토링 설계)
    - frontend/src/components/ui/button.tsx (shadcn Button 컴포넌트 사용 가능 여부 확인)
  </read_first>
  <action>
    `frontend/src/components/chat/chat-message.tsx`를 수정한다. 기존 ReactMarkdown/복사/즐겨찾기 패턴은 그대로 유지하고, 아래 항목을 추가한다:

    ### (A) import 보강
    기존 imports 아래에 추가:
    ```typescript
    import type { ParsedError } from "@/lib/error-messages";
    import { RotateCcw } from "lucide-react";
    ```
    (lucide-react는 이미 Scale/User/Copy/Check/Star 사용 중이므로 RotateCcw 추가만 하면 됨)

    ### (B) Props interface 확장
    기존 `ChatMessageProps`에 3개 optional prop 추가:
    ```typescript
    interface ChatMessageProps {
      role: "user" | "assistant";
      content: string;
      id?: string;
      isFavorite?: boolean;
      onToggleFavorite?: (id: string) => void;
      // Phase 2 D-07: 인라인 에러 배너 + 재시도 버튼
      error?: ParsedError;
      onRetry?: () => void;
      isRetryDisabled?: boolean;
    }
    ```
    함수 인수 destructuring도 동일하게 확장:
    ```typescript
    export function ChatMessage({
      role,
      content,
      id,
      isFavorite,
      onToggleFavorite,
      error,
      onRetry,
      isRetryDisabled,
    }: ChatMessageProps) {
    ```

    ### (C) 인라인 에러 배너 렌더링 (assistant에만, 메시지 bubble 하단에 스택)
    기존 assistant bubble 렌더 블록(`{isUser ? ... : <div className="prose...">...</div>}`) 바로 아래, 그리고 "액션 버튼 (봇 메시지에만)" 블록 바로 **위**에 새 에러 배너를 조건부 렌더:

    ```typescript
    {/* Phase 2 D-07: 인라인 에러 배너 — 실패한 assistant bubble 내부에 rendered */}
    {!isUser && error && (
      <div className="rounded-xl border border-destructive/30 bg-destructive/5 p-3 flex flex-col gap-2">
        <p className="text-[length:var(--text-sm)] text-destructive font-medium">
          {error.message}
        </p>
        {onRetry && (
          <button
            type="button"
            onClick={onRetry}
            disabled={isRetryDisabled}
            className={cn(
              "inline-flex items-center gap-1.5 self-start rounded-md border border-border bg-background px-3 py-1.5 text-[length:var(--text-xs)] font-medium",
              "hover:bg-muted transition-colors",
              "disabled:opacity-50 disabled:cursor-not-allowed"
            )}
          >
            <RotateCcw className="h-3 w-3" />
            다시 시도
          </button>
        )}
      </div>
    )}
    ```

    이 배너는 **message bubble과 나란히 세로 스택**된다. content가 있으면 content + 배너가 함께 보이고, content가 비어 있으면(Ex: mid-stream 에러로 text part가 없음) 배너만 보인다.

    ### (D) **Required**: content가 empty일 때 bubble wrapper 조건부 렌더 (pre-stream 에러에서 빈 말풍선 방지)
    **이 항목은 D-07 UX의 필수 부분이다.** Pre-stream 에러 경로(createMCPClient 실패,
    degraded mode)에서 assistant 메시지의 text part가 아예 없는 상태로 에러 배너만
    렌더되어야 한다. 이 가드를 생략하면 빈 rounded-2xl 말풍선 + 그 아래 에러 배너가
    나란히 나타나 D-07이 정의한 UX가 깨진다. 실행자는 이 Part D를 **skip하지 말 것**.

    기존 코드는 `content`가 빈 문자열이어도 빈 `<div className="rounded-2xl...">`가 렌더됨. 에러 케이스에서 어색한 빈 말풍선을 방지하기 위해, bubble wrapper를 조건부로 렌더:
    ```typescript
    {(content || isUser) && (
      <div
        className={cn(
          "rounded-2xl px-4 py-3 text-[length:var(--text-base)]",
          isUser
            ? "bg-primary text-primary-foreground"
            : "bg-card border border-border"
        )}
      >
        {/* 기존 isUser ? <p>…</p> : <div className="prose…">…</div> 내용 */}
      </div>
    )}
    ```
    (이 조건은 "사용자 메시지는 항상 보이고, 봇 메시지는 text content가 있을 때만 bubble 렌더. 에러만 있을 경우 에러 배너만 보임" 을 의미)

    **단, `"검색 중..."` 플레이스홀더 동작은 건드리지 않는다** — chat-container가 `content="검색 중..."`로 호출할 때 bubble이 보여야 한다. 위 조건 `(content || isUser)`는 "검색 중..."을 truthy로 판정하므로 기존 동작 보존.

    ### (E) 액션 버튼 (복사/즐겨찾기) 조건 유지
    기존 `{!isUser && content && content !== "검색 중..." && (...)}`는 그대로 유지. 에러 케이스(`content === ""` + `error` 존재)에서는 액션 버튼이 숨겨짐 — 기존 동작과 정합.

    ### 파일 외 변경 금지
    - chat-message.tsx 외 파일 수정 금지 (Task 02-02-03/04에서 별도 처리)
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "import type { ParsedError }" frontend/src/components/chat/chat-message.tsx` → 1
    - `grep -c "RotateCcw" frontend/src/components/chat/chat-message.tsx` → ≥ 1 (import + 사용)
    - `grep -c "error\\?:" frontend/src/components/chat/chat-message.tsx` → ≥ 1 (interface field)
    - `grep -c "onRetry\\?:" frontend/src/components/chat/chat-message.tsx` → ≥ 1
    - `grep -c "isRetryDisabled" frontend/src/components/chat/chat-message.tsx` → ≥ 2 (prop decl + 사용처)
    - `grep -c "다시 시도" frontend/src/components/chat/chat-message.tsx` → 1
    - `grep -c "bg-destructive/5" frontend/src/components/chat/chat-message.tsx` → 1
    - `grep -c "border-destructive/30" frontend/src/components/chat/chat-message.tsx` → 1
    - `grep -c "검색 중..." frontend/src/components/chat/chat-message.tsx` → 1 (기존 플레이스홀더 가드 보존)
    - `grep -c "onToggleFavorite" frontend/src/components/chat/chat-message.tsx` → ≥ 2 (기존 기능 보존)
    - **Part D (bubble wrapper 조건부 렌더) 필수 검증**:
      - `grep -Ec "\(content \|\| isUser\)|\(isUser \|\| content\)" frontend/src/components/chat/chat-message.tsx` → ≥ 1 (bubble wrapper 감싸는 조건식 존재)
      - 또는 fallback grep: `grep -c "(content" frontend/src/components/chat/chat-message.tsx` → ≥ 1
      - 이 grep이 0을 반환하면 Part D가 누락된 것이며 태스크는 실패로 간주한다
    - `cd frontend && npx tsc --noEmit` exit 0
  </acceptance_criteria>
  <done>ChatMessage가 3개 신규 prop을 받아 인라인 에러 배너 + 다시 시도 버튼을 렌더한다. Part D(bubble wrapper 조건부 렌더)가 반영되어 pre-stream 에러 시 빈 말풍선이 나타나지 않는다. 기존 복사/즐겨찾기/플레이스홀더 동작은 완전 보존. tsc 통과.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 02-02-03: MessagePartRenderer pass-through + chat-container.tsx 에러 라우팅 재구성 (D-07, D-09, D-10, pre-stream fallback)</name>
  <files>frontend/src/components/chat/message-part-renderer.tsx, frontend/src/components/chat/chat-container.tsx</files>
  <read_first>
    - frontend/src/components/chat/message-part-renderer.tsx (전체, 현재 194 lines)
    - frontend/src/components/chat/chat-container.tsx (전체, 현재 319 lines — 특히 190-203 global error block)
    - frontend/src/lib/error-messages.ts (Task 02-02-01)
    - frontend/src/components/chat/chat-message.tsx (Task 02-02-02 수정본)
    - .planning/phases/02-streaming-stability-error-ux/02-CONTEXT.md D-07, D-09, D-10
    - .planning/phases/02-streaming-stability-error-ux/02-RESEARCH.md §4.3 (isLastAssistant 주입 패턴), §5.2 (pre-stream regenerate fallback), Q3, Q5
  </read_first>
  <action>
    두 파일을 한 태스크에서 동시에 수정한다 (MessagePartRenderer가 ChatContainer의 호출부와 논리적으로 단일 단위이므로 분리하면 컴파일 실패 상태를 유발함).

    ### PART A: `frontend/src/components/chat/message-part-renderer.tsx` 수정

    #### A-1. import 보강
    기존 ui-message-parts import 아래에 추가:
    ```typescript
    import type { ParsedError } from "@/lib/error-messages";
    ```

    #### A-2. Props interface 확장
    ```typescript
    interface MessagePartRendererProps {
      message: ExtractableMessage;
      isFavorite?: boolean;
      onToggleFavorite?: (id: string) => void;
      // Phase 2 D-07: 마지막 assistant 메시지에만 parent가 전달.
      error?: ParsedError;
      onRetry?: () => void;
      isRetryDisabled?: boolean;
    }
    ```
    함수 destructuring도 확장:
    ```typescript
    export function MessagePartRenderer({
      message,
      isFavorite,
      onToggleFavorite,
      error,
      onRetry,
      isRetryDisabled,
    }: MessagePartRendererProps) {
    ```

    #### A-3. Pass-through 3곳
    이 컴포넌트는 3개의 ChatMessage 렌더 경로를 가진다 — Legacy, User, Assistant(텍스트 포함). **3곳 모두** 에러 prop을 pass-through. (assistant가 최종 타겟이지만 type 안전성을 위해 uniform pass-through)

    1) Legacy path (라인 ~47-57):
    ```typescript
    return (
      <ChatMessage
        id={legacy.id}
        role={legacy.role}
        content={legacy.content}
        isFavorite={isFavorite}
        onToggleFavorite={onToggleFavorite}
        error={error}
        onRetry={onRetry}
        isRetryDisabled={isRetryDisabled}
      />
    );
    ```

    2) User path (라인 ~64-73):
    ```typescript
    return (
      <ChatMessage
        id={uiMessage.id}
        role="user"
        content={extractAssistantText(uiMessage)}
        isFavorite={isFavorite}
        onToggleFavorite={onToggleFavorite}
        // user bubble에는 error prop을 전달하지 않음 — parent가 isLastAssistant일 때만 전달.
      />
    );
    ```
    User path에는 error prop을 **고의적으로 pass-through 하지 않는다** (user bubble에 에러 배너가 뜨면 안 됨). chat-container가 이를 보장(isLastAssistant 체크).

    3) Assistant text path (라인 ~121-131):
    ```typescript
    {textChunks.length > 0 && (
      <ChatMessage
        id={uiMessage.id}
        role="assistant"
        content={textChunks.join("")}
        isFavorite={isFavorite}
        onToggleFavorite={onToggleFavorite}
        error={error}
        onRetry={onRetry}
        isRetryDisabled={isRetryDisabled}
      />
    )}
    ```

    #### A-4. textChunks.length === 0 && error 있는 경우 (partial fail)
    기존 구조는 `textChunks.length === 0` 일 때 ChatMessage를 렌더하지 않음. 그러면 에러 prop도 사라짐. 보정:
    assistant path 말미 return 블록을 아래로 수정 — error가 있는 assistant이면 빈 content라도 ChatMessage를 렌더:

    ```typescript
    return (
      <>
        {(textChunks.length > 0 || error) && (
          <ChatMessage
            id={uiMessage.id}
            role="assistant"
            content={textChunks.join("")}
            isFavorite={isFavorite}
            onToggleFavorite={onToggleFavorite}
            error={error}
            onRetry={onRetry}
            isRetryDisabled={isRetryDisabled}
          />
        )}
        {nonTextNodes.length > 0 && (
          <div className="mx-auto max-w-3xl flex flex-wrap gap-2 pl-11 pb-2">
            {nonTextNodes}
          </div>
        )}
      </>
    );
    ```

    ### PART B: `frontend/src/components/chat/chat-container.tsx` 수정

    #### B-1. import 보강
    ```typescript
    import { parseChatError, type ParsedError } from "@/lib/error-messages";
    ```
    (ChatMessage import는 "검색 중..." 플레이스홀더 때문에 유지)

    #### B-2. useChat에서 regenerate, clearError 추가 추출
    기존:
    ```typescript
    const { messages, sendMessage, status, error } = useChat({
      id: conversationId,
    });
    ```
    으로 되어 있는 라인(약 36-38)을 다음으로 교체:
    ```typescript
    const { messages, sendMessage, status, error, regenerate, clearError } = useChat({
      id: conversationId,
    });
    ```
    **useChat의 단일 인수(`{ id: conversationId }`) 호출은 절대 건드리지 않는다** — Phase 4 PERS-03 경계.

    #### B-3. handleRetry helper 추가
    기존 handleExport 함수 정의 **위**에 새 함수 추가 (handleToggleFavorite 근처):
    ```typescript
    const handleRetry = useCallback(async () => {
      // D-09: 실패한 assistant 턴 재생성. Pre-stream 에러 케이스를 대비해
      // 마지막 메시지 역할을 확인한 뒤 regenerate()와 sendMessage() 중 선택.
      // (RESEARCH §5.2 Q3: pre-stream에서 regenerate() 동작 불확실 → fallback 필수)
      clearError();
      const last = messages[messages.length - 1];
      if (last?.role === "assistant") {
        // Mid-stream 에러: 마지막 assistant 메시지를 regenerate.
        await regenerate({ body: { modelId } });
        return;
      }
      // Pre-stream 에러: 마지막 user 메시지를 sendMessage로 재전송.
      if (last?.role === "user") {
        const text = extractAssistantText(last);
        if (!text) return;
        sendMessage({ text }, { body: { modelId } });
      }
    }, [clearError, messages, regenerate, sendMessage, modelId]);
    ```

    #### B-4. parsed error 계산
    messages.map 블록 **위**에 parsed error 변수 추가:
    ```typescript
    const parsedError: ParsedError | undefined = error ? parseChatError(error) : undefined;
    const lastMessage = messages[messages.length - 1];
    const lastIsAssistant = lastMessage?.role === "assistant";
    ```

    #### B-5. messages.map에 조건부 error 주입
    기존 (약 177-184):
    ```typescript
    {messages.map((m) => (
      <MessagePartRenderer
        key={m.id}
        message={m}
        isFavorite={favorites.has(m.id)}
        onToggleFavorite={handleToggleFavorite}
      />
    ))}
    ```
    을 다음으로 교체:
    ```typescript
    {messages.map((m, idx) => {
      const isLast = idx === messages.length - 1;
      const attachedError =
        parsedError && isLast && m.role === "assistant" ? parsedError : undefined;
      return (
        <MessagePartRenderer
          key={m.id}
          message={m}
          isFavorite={favorites.has(m.id)}
          onToggleFavorite={handleToggleFavorite}
          error={attachedError}
          onRetry={attachedError ? handleRetry : undefined}
          isRetryDisabled={isLoading}
        />
      );
    })}
    ```

    #### B-6. Pre-stream 에러 standalone bubble (RESEARCH Q5 옵션 A)
    `{isLoading && messages[messages.length - 1]?.role === "user" && ( <ChatMessage role="assistant" content="검색 중..." /> )}` 블록 **바로 아래** (map 렌더 루프의 바깥이지만 max-w-3xl 컨테이너 안), error 상태 + 마지막 메시지가 user일 때만 standalone 에러 버블 렌더:
    ```typescript
    {parsedError && !lastIsAssistant && (
      <ChatMessage
        role="assistant"
        content=""
        error={parsedError}
        onRetry={handleRetry}
        isRetryDisabled={isLoading}
      />
    )}
    ```

    #### B-7. 기존 global error block 완전 삭제
    chat-container.tsx 현재 `{error && (\n    <div className="mx-auto max-w-3xl px-4 pb-4">\n      <div className="rounded-xl border border-destructive/30 bg-destructive/5 p-4">\n        <p className="text-sm font-medium text-destructive mb-1">오류가 발생했습니다</p>\n        …\n      </div>\n    </div>\n  )}` 블록(약 190-203) 전체를 삭제한다.

    검증 문자열:
    - `grep -c "오류가 발생했습니다" frontend/src/components/chat/chat-container.tsx` 가 0이 되어야 함.
    - `grep -c "error.message?.includes" frontend/src/components/chat/chat-container.tsx` 가 0이 되어야 함.
    - `grep -c "혼잡" frontend/src/components/chat/chat-container.tsx` 가 0이 되어야 함 (문자열은 error-messages.ts가 소유).

    #### B-8. Phase 4 경계 보존 확인
    - `useChat({ id: conversationId })` 단일 인수 호출 그대로
    - `lib/conversations.ts` 수정 금지 (이 플랜은 건드리지 않음)
    - initialMessages prop/useEffect localStorage 저장 로직 그대로 유지
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit && npm run build</automated>
  </verify>
  <acceptance_criteria>
    ### message-part-renderer.tsx
    - `grep -c "import type { ParsedError }" frontend/src/components/chat/message-part-renderer.tsx` → 1
    - `grep -c "error\\?: ParsedError" frontend/src/components/chat/message-part-renderer.tsx` → 1
    - `grep -c "onRetry\\?:" frontend/src/components/chat/message-part-renderer.tsx` → ≥ 1
    - `grep -c "isRetryDisabled" frontend/src/components/chat/message-part-renderer.tsx` → ≥ 2
    - `grep -c "error={error}" frontend/src/components/chat/message-part-renderer.tsx` → ≥ 2 (legacy + assistant paths)
    - `grep -c "role=\"user\"" frontend/src/components/chat/message-part-renderer.tsx` → ≥ 1 (user path 존재 확인)
    - User path의 ChatMessage 호출에는 `error=` prop이 없음: user path 주변 20줄 내에 `error=` 가 없어야 함 (수동 확인; tsc로도 체크 가능)
    - `grep -c "textChunks.length > 0 || error" frontend/src/components/chat/message-part-renderer.tsx` → 1 (partial fail 경로)

    ### chat-container.tsx
    - `grep -c "parseChatError" frontend/src/components/chat/chat-container.tsx` → ≥ 2 (import + 사용)
    - `grep -c "regenerate" frontend/src/components/chat/chat-container.tsx` → ≥ 2 (useChat destructure + handleRetry 내부 호출)
    - `grep -c "clearError" frontend/src/components/chat/chat-container.tsx` → ≥ 2
    - `grep -c "handleRetry" frontend/src/components/chat/chat-container.tsx` → ≥ 3 (정의 + map 내 + standalone bubble)
    - `grep -c "parsedError" frontend/src/components/chat/chat-container.tsx` → ≥ 3
    - `grep -c "attachedError" frontend/src/components/chat/chat-container.tsx` → ≥ 2
    - `grep -c "lastIsAssistant" frontend/src/components/chat/chat-container.tsx` → ≥ 2
    - `grep -c "useChat({\\s*id: conversationId" frontend/src/components/chat/chat-container.tsx` → 1 (Phase 4 경계 보존)
    - **Global error block 완전 제거**:
      - `grep -c "오류가 발생했습니다" frontend/src/components/chat/chat-container.tsx` → **0**
      - `grep -c "error.message?.includes" frontend/src/components/chat/chat-container.tsx` → **0**
      - `grep -c "혼잡" frontend/src/components/chat/chat-container.tsx` → **0**
      - `grep -c "429" frontend/src/components/chat/chat-container.tsx` → **0**
    - lib/conversations.ts 미수정: `git diff HEAD frontend/src/lib/conversations.ts` → empty diff
    - `ChatMessage role="assistant" content="검색 중..."` 블록 여전히 존재 (플레이스홀더 보존)

    ### 빌드
    - `cd frontend && npx tsc --noEmit` exit 0
    - `cd frontend && npm run build` exit 0
  </acceptance_criteria>
  <done>
    MessagePartRenderer는 error/onRetry/isRetryDisabled 3개 prop을 pass-through하고, ChatContainer는 global error block을 제거한 뒤 parsed error를 마지막 assistant 또는 pre-stream standalone bubble로 라우팅한다. handleRetry는 regenerate + sendMessage fallback 두 경로를 모두 구현. TypeScript + Next 빌드 통과.
  </done>
</task>

</tasks>

<verification>
## Plan-Level Gate (must_haves 증거 수집)

```bash
cd frontend

# 1. 타입/빌드
npx tsc --noEmit                                                      # exit 0
npm run build                                                          # exit 0, "Compiled successfully"

# 2. 새 파일 존재
ls -l src/lib/error-messages.ts                                       # OK

# 3. error-messages.ts 계약
grep -c "export type ErrorCode" src/lib/error-messages.ts             # 1
grep -c "parseChatError" src/lib/error-messages.ts                    # ≥ 2
grep -c "KOREAN_MESSAGES" src/lib/error-messages.ts                   # ≥ 2
for c in mcp_timeout mcp_busy mcp_offline stream_timeout unknown; do
  grep -c "$c" src/lib/error-messages.ts                              # ≥ 2
done
grep -c "\\[⚠️ 미확인 답변\\]" src/lib/error-messages.ts              # ≥ 2

# 4. chat-message.tsx 인라인 배너
grep -c "다시 시도" src/components/chat/chat-message.tsx               # 1
grep -c "bg-destructive/5" src/components/chat/chat-message.tsx       # 1
grep -c "ParsedError" src/components/chat/chat-message.tsx            # ≥ 2
grep -c "isRetryDisabled" src/components/chat/chat-message.tsx        # ≥ 2
grep -c "검색 중..." src/components/chat/chat-message.tsx              # 1

# 5. message-part-renderer.tsx pass-through
grep -c "ParsedError" src/components/chat/message-part-renderer.tsx   # ≥ 2
grep -c "error={error}" src/components/chat/message-part-renderer.tsx # ≥ 2

# 6. chat-container.tsx 리라우팅
grep -c "parseChatError" src/components/chat/chat-container.tsx       # ≥ 2
grep -c "regenerate" src/components/chat/chat-container.tsx           # ≥ 2
grep -c "clearError" src/components/chat/chat-container.tsx           # ≥ 2
grep -c "handleRetry" src/components/chat/chat-container.tsx          # ≥ 3
grep -c "attachedError" src/components/chat/chat-container.tsx        # ≥ 2
grep -c "lastIsAssistant" src/components/chat/chat-container.tsx      # ≥ 2

# 7. 기존 global error block 완전 제거
grep -c "오류가 발생했습니다" src/components/chat/chat-container.tsx   # 0
grep -c "error.message?.includes" src/components/chat/chat-container.tsx  # 0
grep -c "혼잡" src/components/chat/chat-container.tsx                  # 0

# 8. Phase 4 경계 보존
grep -c "useChat({" src/components/chat/chat-container.tsx            # 1 (single-arg 호출)
git diff HEAD -- src/lib/conversations.ts | wc -l                      # 0 (수정 없음)

# 9. Phase 1 자산 보존
grep -c "검색 중..." src/components/chat/chat-container.tsx            # 1 (플레이스홀더 유지)
grep -c "extractAssistantText" src/components/chat/chat-container.tsx  # ≥ 2 (Phase 1 single-source)
```
</verification>

<success_criteria>
- 4개 파일 수정 (1 new, 3 modified) 모두 commit 준비 완료
- `cd frontend && npx tsc --noEmit && npm run build` 통과
- 기존 global error block 모든 흔적(오류가 발생했습니다 / 혼잡 / error.message.includes) 완전 제거
- Phase 1 자산(extractAssistantText 단일 source, 검색 중 플레이스홀더, useChat 단일 인수) 완전 보존
- Plan 02-03 UAT에서 Playwright로 각 실패 모드의 UI 렌더링을 검증 가능한 상태
</success_criteria>

<output>
After completion, create `.planning/phases/02-streaming-stability-error-ux/02-02-SUMMARY.md` documenting:
- 수정한 파일 4개와 각 파일의 line delta
- ChatMessage의 bubble wrapper 조건 변경 이유 (빈 bubble 방지)
- pre-stream vs mid-stream 경로 DOM 배치 결정 (standalone vs 인라인)
- handleRetry의 regenerate/sendMessage 분기 근거 (RESEARCH Q3)
- Deviations (있다면)
</output>